# 미니 프로젝트 1 수행일지 — 8/21 (금)

> 이 문서는 **8월 21일 하루치만** 담습니다.
> 8/19~8/20 기록은 `[1일차_제출용] MP1_수행일지-iny.md` 에 있습니다.

---

**결과물**

| 항목 | 주소 |
|---|---|
| GitHub | https://github.com/wkddlsdud971014-young/iny-senior-cert-apply |
| 배포 사이트 | https://iny-senior-cert-apply.vercel.app |

**오늘 커밋**

| | |
|---|---|
| `e9babc6` | fix: 접수 화면에서 잘못된 연락처가 빠져나가던 통로 4개를 막음 |
| `3aeeee8` | feat: 챗봇 답변을 3단계로 나눠 답할 수 있는 것까지 끊기지 않게 함 |

파일 8개 · 816줄 추가 · 168줄 삭제

---

**오늘 목표**

어제 사용성 테스트 회고에서 정한 MoSCoW 우선순위 가운데 MUST 네 가지를 처리한다.
어제 "원인을 못 찾았다"고 적어 둔 두 가지를 코드에서 찾아 없앤다.

1. 전화번호 입력값이 수정·Enter 과정에서 Validation 을 우회하는 원인
2. 어드민의 개선 우선순위를 판단할 근거가 없다는 점

**한 일** (한 줄씩)

- 코드를 읽어 전화번호 검증이 빠져나가는 자리 네 곳을 찾아냄 — 어제의 미해결 항목 1
- 검증 규칙을 `phone.js` 한 파일로 모으고, 접수 화면과 접수 확인 화면이 같이 쓰게 함
- 모든 입력 칸에 네 가지 상태(입력 전·입력 중·정상 완료·오류)를 붙임
- 챗봇 답변을 3단계(답변 가능 / 조건부 답변 가능 / 답변 불가)로 나눔
- 챗봇 안내 문구를 코드가 아니라 `faq_docs` 에 두어 담당자가 고칠 수 있게 함
- 고치다 원래부터 죽어 있던 규칙 두 개를 발견해 함께 고침
- 문서에 섞여 있던 직원용 주의사항이 어르신께 그대로 읽히던 것을 고침
- 커밋·배포 후 실제 사이트에서 전 항목 확인

---

## 어제 못 찾은 것을 오늘 찾음 — 전화번호 우회 통로 4개

어제 일지 "장애 요소"에 이렇게 적었다.

> 전화번호 입력값이 수정·Enter 입력 과정에서 Validation 을 우회하는 원인을
> 아직 정확히 파악하지 못했습니다.

코드를 읽으니 빠져나가는 자리가 하나가 아니라 **넷**이었다.
하나만 막았으면 나머지 셋으로 계속 새어 나갔을 것이다.

### ① 연락처 칸의 엔터가 검사를 부르지 않았다

`starter/script.js`

이름 칸의 엔터는 `checkName()` 을 부른다. 그런데 연락처 칸의 엔터는 그냥 넘긴다.

```js
// 고치기 전
phoneBox.addEventListener("keydown", function (e) {
  if (e.key !== "Enter" || isTypingHangul(e)) return;
  e.preventDefault();
  goTo(agP);          // ← checkPhone() 을 부르지 않는다
});
```

같은 화면인데 칸마다 규칙이 달랐다. 이름은 막히고 연락처는 안 막혔다.

**배운 것** — 검사를 "어디서 부를지"를 칸마다 따로 적으면 반드시 빠뜨린다.
이번에 검사·엔터·수정·보내기 네 자리가 모두 같은 함수 하나를 부르게 바꿨다.

### ② 자릿수를 넘긴 숫자가 말없이 사라졌다

하이픈을 붙이는 함수가 11자리까지만 남기고 버렸다.

```js
return d.slice(0, 3) + "-" + d.slice(3, 7) + "-" + d.slice(7, 11);
//                                                            ↑ 12번째부터 사라짐
```

숫자를 12개 넣으면 12번째부터 **화면에서 조용히 없어진다.**
어르신은 다 넣은 줄 아신다. 검사는 11자리로 통과한다.

**배운 것** — 이건 검증 문제이기 전에 **원본 불변 원칙 위반**이었다.
"저장된 원본을 고치지 않는다" 는 지켰는데, **화면에서 이미 값을 고치고 있었다.**
회고에 적었던 대로다 — 원본 보존과 데이터 정확성은 같은 문제가 아니다.

지금은 넘긴 숫자를 받지 않되, 사라졌다는 사실을 반드시 말한다.
말없이 자르는 것과 막고 알리는 것은 다르다.

### ③ 고치는 중에 오류 표시가 먼저 사라졌다

오류가 뜬 뒤 한 글자만 지우면 `input` 이벤트가 숫자 여부만 보고 오류를 지웠다.

```js
// 고치기 전 — 자릿수를 다시 세지 않고 무조건 지운다
document.getElementById("e-phone").hidden = true;
```

아직 틀린 번호인데 화면은 멀쩡해 보인다. 그대로 보내기를 누르면 다시 막힌다.
**이게 "수정 과정에서 우회된다" 고 느껴진 실제 정체였다.**

**배운 것** — 오류를 언제 띄울지만 정하고 **언제 지울지는 안 정했다.**
지금은 한 번 틀린 칸을 표시해 두고, 제대로 고칠 때까지 계속 다시 센다.

### ④ 번호 형식 자체를 안 봤다

자릿수와 "0으로 시작" 만 봤다. 그래서 없는 번호대가 통과했다.

| 값 | 전 | 후 |
|---|---|---|
| `057-1234-5678` | 통과 | 막힘 (없는 지역번호) |
| `010-1111-1111` | 통과 | 막힘 (같은 숫자만 이어짐) |
| `02-0123-4567` | 통과 | 막힘 (국번이 0으로 시작) |

받는 범위를 정했다 — 휴대폰(`010`·`011`·`016~019`), 지역번호(`02`·`031~064`), 인터넷전화(`070`).

### 덤 — 두 화면이 서로 다르게 판정하고 있었다

접수 화면과 접수 확인 화면이 하이픈 함수를 **각자 갖고 있었다.**
확인 화면 쪽은 02 지역번호를 몰라서, 같은 번호가 화면마다 다르게 보였다.

| | 접수 화면 | 접수 확인 화면 |
|---|---|---|
| `0212345678` | `02-1234-5678` | `021-234-5678` |

어르신은 "내가 잘못 넣었나" 하시게 된다.

**배운 것** — 같은 규칙을 두 곳에 적으면 반드시 어긋난다.
`starter/phone.js` 한 파일로 모았다. 이제 어긋날 수가 없다.

---

## 입력 상태를 네 가지로 통일

회고에서 적은 문제 그대로였다.

- 이름 칸의 "다 넣으시면 엔터를 누르십시오" 가 **다 넣은 뒤에도 남아 있었다**
- 연락처 칸은 안내가 **아예 없었다**
- 동의 오류는 화면 아래 한 곳에만 떠서 **어디가 문제인지 찾아 내려가야 했다**

네 칸 모두 같은 네 가지 상태를 갖게 했다.

| | 입력 전 | 입력 중 | 정상 완료 | 오류 |
|---|---|---|---|---|
| 이름 | 무엇을 넣는지 | "엔터를 누르십시오" | ✓ 홍길동 님으로 접수합니다 | 무엇이 틀렸는지 |
| 연락처 | "숫자만 넣으셔도 됩니다" | "지금 7자리 / 11자리" | ✓ 010-1234-5678 로 연락드립니다 | 몇 자 모자란지 |
| 하실 말씀 | "안 쓰셔도 접수됩니다" | "다 쓰시면 동의로" | ✓ 적어 주셨습니다 | — (선택사항) |
| 동의 | "두 가지에 모두 체크" | "한 가지 더 남았습니다" | ✓ 두 가지 모두 동의하셨습니다 | 어느 쪽이 남았는지 |

**규칙 두 가지를 세웠다.**

1. 한 칸에는 한 줄만 보인다. 안내와 오류가 같이 떠 있지 않는다.
2. 다 넣은 칸에는 시키는 말이 남지 않는다.

빈 채로 보내기를 누르면 전에는 한 칸씩 막혀 세 번 눌러야 했다.
지금은 "3곳을 더 봐 주십시오" 라고 한 번에 알리고 첫 칸으로 데려간다.

**알게 된 것** — "아직 넣고 계시는 중" 과 "틀리게 넣으심" 을 갈라야 한다.
`010` 까지만 치신 시점에 "8자 모자랍니다" 라고 하면 다그치는 것이 된다.
오류를 종류별로 나누고(`모자람`·`넘침`·`없는 번호대`·`같은 숫자`),
**더 넣어도 맞아지지 않는 것만** 즉시 알리게 했다.

---

## 챗봇 답변을 3단계로

### 무엇이 문제였나

회고에 적은 대로 **환각 방지를 우선하다 답할 수 있는 것까지 끊고 있었다.**

전에는 두 갈래였다.

```
근거 있음 → 답변
근거 없음 → 거절
```

그래서 이렇게 됐다.

| 질문 | 전 |
|---|---|
| 제가 합격할 수 있을까요? | 모르겠습니다 |
| 시험장에 주차 되나요? | 모르겠습니다 |
| 요양보호사 응시료 얼마예요? | 모르겠습니다 |

합격 여부는 정말 알 수 없다. **그런데 시험 일정과 준비물은 우리가 안다.**
어르신은 "여기서는 아무것도 안 알려주는구나" 하고 끊으셨다.

### 어떻게 바꿨나

가운데 단계를 넣었다.

```
답변 가능        근거가 있다              → 근거대로 답한다
조건부 답변 가능  그건 못 하지만 이건 된다   → 못 하는 것 + 되는 것을 같이 말한다
답변 불가        우리 범위 밖이다          → 안 된다고만 말한다
```

배포된 사이트에서 확인한 실제 답이다.

> **Q. 제가 합격할 수 있을까요?**
> [조건부 답변 가능]
> 합격 여부를 미리 판단해 드릴 수는 없습니다.
> 대신 시험 일정이나 시험 당일 준비물은 알려드릴 수 있습니다.

> **Q. 요양보호사 응시료 얼마예요?**
> [조건부 답변 가능]
> 요양보호사 응시료는 저희가 확인하지 못했습니다.
> 짐작해서 말씀드리면 잘못 안내될 수 있어 알려드리지 않습니다.
> 대신 어디서 접수하는지, 언제까지 접수하는지는 알려드릴 수 있습니다.

**지어내지 않는다는 원칙은 그대로다.** 조건부도 새 사실을 만들지 않는다.
못 하는 것은 여전히 못 한다고 말하고, 할 수 있는 일을 덧붙일 뿐이다.

### 문구를 어디에 둘지

**"언제 끊을지" 는 코드가 정하고, "뭐라고 말할지" 는 담당자가 고친다.**

거절 문구를 표에 둔 것과 같은 기준이다.
안내문 4건을 `faq_docs` 에 넣었다 (`starter/supabase_faq_notice.sql`).
표를 못 읽어도 코드에 적힌 기본 문구로 그대로 돈다. 챗봇이 멈추지 않는다.

---

## 고치다 발견한 것 — 원래부터 죽어 있던 규칙 2개

이건 오늘 만든 문제가 아니다. **전부터 있었는데 아무도 몰랐다.**

### ① 두 낱말짜리 규칙이 한 번도 맞은 적이 없다

질문은 띄어쓰기를 지우고 비교하는데, **찾는 낱말 쪽은 안 지웠다.**

```
질문   "지게차랑 굴착기 같이 딸 수 있나요?"
     → "지게차랑굴착기같이딸수있나요?"     (띄어쓰기 지움)
규칙   "같이 딸"                            (띄어쓰기 그대로)
결과   영영 안 맞음
```

`"다른 자격증"`, `"같이 딸"` 두 규칙이 처음부터 죽어 있었다.

**알게 된 것** — 양쪽을 같은 모양으로 만들어야 비교가 된다.
한쪽만 다듬으면 조용히 안 걸린다. **오류가 안 나서 더 위험하다.**

### ② 실습본이 배포본보다 훨씬 넓게 막고 있었다

`chatbot/app.py` 는 `"합격"` 만 들어가도 끊었다.

| 질문 | 실습본 | 배포본 |
|---|---|---|
| 필기 **합격**은 몇 년 유효한가요? | 거절 ❌ | 답변 ✅ |

문서에 답이 있는 질문인데 거절했다.
**회고에서 지적한 "과도한 답변 차단" 의 실제 사례가 여기 있었다.**

두 파일 첫머리에 "똑같은 규칙으로 답합니다" 라고 적어 뒀는데 사실이 아니었다.
같은 낱말(`"합격할"`)로 맞췄다.

**알게 된 것** — 주석에 "두 곳이 같다" 고 적어 두는 것만으로는 같아지지 않는다.
확인하지 않으면 조용히 갈라진다.

---

## 문서에 섞인 직원용 주의사항이 어르신께 읽히고 있었다

배포된 챗봇에 물었더니 이렇게 답했다.

> **Q. 필기 합격은 몇 년 유효한가요?**
> 국가기술자격 기능사는 필기 합격이 2년간 유효합니다.
> **자격증마다 다르므로 뭉뚱그려 2년이라고 답하면 안 됩니다.** ← 이 줄

뒷줄은 어르신께 드리는 말이 아니다.
발주 문서(`02_안내규정.md` 156줄)에 적힌 **우리끼리 보는 주의사항**이다.

> **뭉뚱그려 "2년"이라고 답하면 안 됩니다.** 자격증마다 다릅니다.

문서를 옮겨 적을 때 이 줄까지 같이 들어갔고, 챗봇이 그대로 읽었다.
어르신은 "나더러 답하지 말라는 건가" 하시게 된다.

**그냥 지우지 않았다.** 그 줄이 담은 사실은 맞다 — 공인중개사는 기간이 다르다.
지우기만 하면 "공인중개사도 2년" 으로 잘못 아시게 된다.
같은 사실을 어르신께 드리는 말로 바꿔 적었다.

```
공인중개사는 기간이 다릅니다.
공인중개사 면제 기간은 저희가 확인하지 못했으니 공식 사이트에서 확인해 주십시오.
```

공인중개사 면제 기간은 발주처가 확인하지 못한 8가지 가운데 하나다.
그러니 기간을 적지 않고 확인하지 못했다고 밝혔다. 짐작해서 채우지 않는다.

**배운 것** — 발주 문서에는 두 가지가 섞여 있다.

| | 어떻게 |
|---|---|
| 어르신께 드릴 말 | 문서에 옮긴다 |
| 우리끼리 볼 주의사항 | 옮기지 않는다. 코드나 주석에 남긴다 |

옮기기 전에 **"이 문장을 어르신이 읽으셔도 되는가"** 를 한 번 보면 된다.
같은 기준으로 문서 21건을 훑었고 이 한 건만 걸렸다.

---

## 배포하다 알게 된 것 — GitHub 자동 배포가 연결돼 있지 않았다

커밋하고 GitHub 에 올렸는데 사이트가 그대로였다.
6분을 기다려도 안 바뀌길래 확인해 보니 이랬다.

| 커밋 | Vercel 배포 기록 |
|---|---|
| `3aeeee8` (오늘) | 0건 |
| `d14fe77` (어제) | 0건 |
| `8f002a2` (어제) | 0건 |
| `42aa5d5` (어제) | 0건 |

**한 번도 GitHub 를 통해 배포된 적이 없었다.** 전부 터미널에서 직접 올린 것이었다.

Vercel CLI 도 다른 계정(`iny101400-young`)으로 로그인돼 있어서
`wkddlsdud971014` 계정으로 다시 로그인한 뒤 `vercel --prod` 로 올렸다.

**알게 된 것** — "커밋했다"·"push 했다"·"배포됐다" 는 서로 다른 일이다.
연결이 없으면 앞의 둘을 아무리 해도 사이트는 안 바뀐다.
배포한 뒤에는 반드시 **실제 주소에 들어가서** 확인해야 한다.

---

## 배포 범위를 확인하다 — SQL 파일 4개가 인터넷에 열려 있었다

일지와 발주 문서가 사이트에 안 올라가는지 확인하다가 다른 것을 봤다.

| | 배포 사이트 |
|---|---|
| 일지 · `client-docs/` · `QA1/` · `chatbot/` | 404 (막힘 ✅) |
| **`starter/` 안의 SQL 6개 중 4개** | **200 (열려 있음 ❌)** |

주소만 치면 표 이름과 컬럼 구조가 그대로 보였다.

```
supabase_faq.sql              200
supabase_faq_notice.sql       200
supabase_faq_내부메모_제거.sql   200
supabase_staff.sql            200
```

`.vercelignore` 에 **파일 이름을 하나씩 적어 둔 것**이 원인이었다.
처음 두 개만 적혀 있었고, 그 뒤에 만든 것들은 적는 것을 잊었다.

```
/supabase.sql
/supabase_integrated.sql
        ← 뒤에 만든 네 개는 여기 없음
```

**배운 것** — 막을 것을 이름으로 적으면 **새로 만들 때마다 잊는다.**
잊어도 오류가 안 난다. 그냥 조용히 열린다.
확장자(`*.sql`)로 바꿨다. 앞으로 SQL 을 더 만들어도 저절로 빠진다.

어제 일지 12번에 `.vercelignore` 에 파일 이름만 적었다가
`api/config.js` 까지 빠져 함수가 사라진 일을 적어 뒀다.
그때는 **너무 많이 막혀서** 탈이 났고, 이번은 **너무 적게 막혀서** 탈이 났다.
같은 파일에서 반대 방향으로 두 번 틀린 셈이다.

배포한 뒤 확인했다 — SQL 6개 전부 404, 화면 8개와 `/api/chat` 은 그대로 200.

---

## 오늘 고친 파일

| 파일 | 내용 |
|---|---|
| `starter/phone.js` | **새로 만듦** — 연락처 규칙 (두 화면이 같이 씀) |
| `starter/script.js` | 우회 통로 4개 차단 · 상태 4단계 |
| `starter/apply.html` | 상태 표시 자리(안내/정상/오류) |
| `starter/check.html` | 하이픈 함수 중복 제거 · 공용 파일 사용 |
| `starter/style.css` | 정상 완료 표시 · 깨진 색값(`#A99madeup`) 정리 |
| `starter/api/chat.js` | 답변 3단계 · 띄어쓰기 비교 버그 |
| `chatbot/app.py` | 배포본과 같은 규칙으로 맞춤 |
| `chatbot/faq.json` | 직원용 주의사항 제거 |
| `starter/supabase_faq.sql` | 같은 내용 반영 |
| `starter/supabase_faq_notice.sql` | **새로 만듦** — 안내문 4건 |
| `starter/supabase_faq_내부메모_제거.sql` | **새로 만듦** — 문서 1건 수정 |
| `starter/.vercelignore` | SQL 을 이름 대신 확장자로 막음 |

---

## 확인한 것

| 무엇을 | 어떻게 | 결과 |
|---|---|---|
| 연락처 규칙 | 통과 10건 + 막힘 11건 | 21건 전부 맞음 |
| 접수 화면 우회 | 진짜 화면을 띄워 8가지 상황 재현 | 8건 전부 막힘 |
| 입력 상태 4단계 | 네 칸 × 상태별 확인 | 전부 맞음 |
| 챗봇 3단계 | 배포된 서버에 직접 질문 | 전부 맞음 |
| 정상 질문이 안 새는지 | 15개 질문 | 하나도 안 샘 |
| 배포된 파일 | 내 컴퓨터와 지문(해시) 비교 | 5개 파일 전부 같음 |
| 공개되면 안 되는 것 | 일지·발주 문서·QA·실습 코드 주소 접속 | 전부 404 |
| SQL 노출 차단 | 6개 주소 접속 | 전부 404 (전에는 4개가 200) |
| 차단 후 사이트 | 화면 8개 + `/api/chat` | 전부 200 |

---

## 아직 안 한 것

### 1. 어드민 가독성 개선 — **일부러 안 했다**

회고의 SHOULD 항목이다. 어제 일지의 장애 요소 2번이기도 하다.

> 어드민은 실제 관리자 관점의 사용성 테스트가 부족해,
> 개선 우선순위를 판단할 근거가 상대적으로 적습니다.

**이 상태는 오늘도 그대로다.** 무엇을 먼저 보여야 할지 정할 근거가 없다.
지금 배치를 바꾸면 짐작으로 고치는 것이 된다.

다음에 할 순서는 회고에 적어 둔 대로다.
관리자 역할을 맡은 사람에게 별도 과제를 주고 **찾는 시간·클릭 수·잘못 누른 자리**를 잰다.

1. 전화번호가 잘못된 접수 건을 찾아 주세요
2. 특정 자격증 신청자만 확인해 주세요
3. 미결제 신청자만 확인해 주세요
4. 특정 사용자의 원본 입력값을 확인해 주세요
5. 챗봇의 특정 안내 문구를 수정해 주세요

### 2. Supabase 문서 수정 SQL 실행

`starter/supabase_faq_내부메모_제거.sql` 을 아직 안 돌렸다.
돌리기 전까지 배포된 챗봇은 계속 "답하면 안 됩니다" 를 읽는다.

### 3. GitHub ↔ Vercel 연결

아직 안 붙였다. 다음에 코드를 고치면 `vercel --prod` 를 직접 쳐야 한다.

### 4. 회고의 SHOULD·COULD 나머지

- 어드민 정보 위계 재설계
- 텍스트 가독성 (thin 계열 줄이기)
- 클릭 가능한 요소의 표현 통일
- 챗봇 말투 부드럽게
- 접수 완료 후 다음 행동 안내 강화

---

## 데일리 스크럼

### 어제 무엇을 했나요?

- 시니어 자격증 접수 서비스의 사용자 테스트 피드백을 정리했습니다.
- 초기 가설과 실제 피드백을 비교해 회고했습니다.
- 개선 항목을 MoSCoW 와 I-E Matrix 기준으로 우선순위화했습니다.

### 오늘 무엇을 했나요?

- 어제 원인을 못 찾았던 전화번호 Validation 우회 통로 **4개를 코드에서 찾아 모두 막았습니다.**
- 두 화면이 따로 갖고 있던 연락처 규칙을 한 파일로 모아 판정이 어긋나지 않게 했습니다.
- 모든 입력 칸에 네 가지 상태를 붙이고, 한 칸에 한 줄만 보이도록 통일했습니다.
- 챗봇 답변을 3단계로 나눠, 못 하는 것과 대신 할 수 있는 것을 같이 말하게 했습니다.
- 고치는 과정에서 **원래부터 죽어 있던 규칙 2개**와, 문서에 섞여 있던 **직원용 주의사항 노출**을 발견해 함께 고쳤습니다.
- 커밋·배포하고 실제 사이트에서 전 항목을 확인했습니다.

### 내일 무엇을 할 예정인가요?

- 관리자 역할 사용성 테스트를 설계하고 실제로 돌립니다. 어드민 개선은 그 결과를 보고 정합니다.
- Supabase 문서 수정 SQL 을 실행합니다.
- GitHub ↔ Vercel 자동 배포를 연결합니다.
- 회고의 SHOULD 항목(텍스트 가독성 · 클릭 표현 통일)을 처리합니다.

### 업무를 방해하는 장애 요소가 있나요?

- **어드민 근거 부족은 어제와 같습니다.** 관리자 관점 테스트를 아직 못 해서, 무엇을 먼저 보여야 할지 정할 수 없습니다. 이건 코드를 더 본다고 풀리는 문제가 아니라 사람에게 시켜 봐야 풀리는 문제입니다.
- GitHub 자동 배포가 연결돼 있지 않아, 배포를 잊으면 고친 것이 사이트에 반영되지 않습니다.

---

## 오늘 가장 크게 바뀐 판단

어제 회고에서 이렇게 적었다.

> 사용자가 끝까지 갈 수 있는 것보다
> 올바르게 끝까지 갈 수 있는지가 더 중요하다.

오늘 코드를 열어 보고 하나를 더 알았다.

**검증은 "넣었느냐" 가 아니라 "빠져나갈 자리를 다 막았느냐" 다.**

`checkPhone()` 은 처음부터 있었다. 보내기를 누르면 제대로 검사도 했다.
그런데 엔터로도, 수정으로도, 자릿수 초과로도 빠져나갔다.
**검증 함수가 있다는 것과 검증이 된다는 것은 다른 말이었다.**

챗봇에서도 같은 일이 있었다.
`"같이 딸"` 규칙은 코드에 **적혀 있었지만 한 번도 동작하지 않았다.**
오류가 안 나니 아무도 몰랐다.

> 코드에 적혀 있는 것과 실제로 도는 것은 다르다.
> 적어 놓고 안심하지 말고, 실제로 그 길로 걸어 봐야 한다.

오늘 진짜 화면을 띄워 8가지 상황을 재현하고, 배포된 서버에 직접 질문을 던진 이유다.
그렇게 하지 않았으면 죽어 있던 규칙 두 개는 오늘도 못 찾았을 것이다.
