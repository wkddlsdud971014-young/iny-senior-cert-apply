# 4Ls 회고 — Day22

**작성일**: 2026-08-19
**주제**: 미니프로젝트 1일차 — 시니어 자격증 접수 서비스 (접수 화면 / 챗봇 / 어드민 / DB 통합)
**상태**: 카드 A~G 완료 ✅ / 인수검사 7개 통과 ✅ / 공개 주소 3개 ✅ / QA 1회차 완료 ✅ / QA 재테스트 대기 ⏳

---

## Liked (좋았던 것)

**이전 실습에서 삽질했던 것들이 이번에는 막히지 않았다.**

같은 함정을 두 번째 만나니 "아 이거"하고 바로 넘어갔다. 삽질이 자산이 된 첫 경험이다.

- **데이터 맘대로 안 지우기** — 자격증 표기 흔들림(`한식` / `한식조리` / `한식조리기능사`)을 DB check 제약으로 막으려다, 그러면 원장의 원본 행이 아예 못 들어온다는 걸 SQL 실행 **전에** 알아챘다.
- **.env 파일 확인하기** — Gemini 키를 받자마자 `.env` 를 만들고 `.gitignore` 에 걸린 걸 눈으로 확인했다. 저장소가 Public이라 한 번 올라가면 되돌릴 수 없다.
- **SQL 실행하기** — 테이블 만들기, 정책 걸기, 트리거 붙이기까지 막힘 없이 갔다.
- **환경변수 입력** — Supabase URL·키, Gemini 키를 Vercel에 넣는 흐름이 손에 익어 있었다.

**계정을 세 번 확인한 것도 좋았다.**

GitHub·Vercel·Supabase 셋 다 시작 전에 활성 계정을 봤고, **세 번 모두 틀린 계정이었다.**
그대로 만들었으면 저장소·배포·DB가 전부 다른 계정에 묶였을 것이다.
특히 GitHub는 이미 만든 뒤에 알아채서 옮겨야 했는데, **지우기 전에 먼저 옮긴 순서**가 맞았다. 지우고 다시 만들면 그 사이에 원본이 없는 구간이 생긴다.

---

## Learned (배운 것)

**완료되었다고 생각하고 한두 번 테스트해서는 오류를 발견하기 어렵다.**

익숙한 방법으로만 눌러보기 때문이다. 내가 만든 사람이라 "이렇게 쓰겠지" 하는 길로만 간다.
QA 체크리스트를 받아 **정해진 항목을 하나씩** 밟으니 그제야 나왔다.

QA 전에는 다 된 줄 알았는데 이런 게 나왔다.

| 발견한 것 | 왜 못 봤나 |
|---|---|
| 이름에 `ㅎ`, `!@#$%`, `12345`, 50자가 전부 접수됨 | 나는 늘 `김두두` 같은 정상 이름만 넣어봤다 |
| 연락처 `010`만 넣어도 접수됨 | 늘 11자리를 다 넣었다 |
| 연락처에 글자를 넣으면 막히긴 하는데 **왜 안 써지는지 안 알려줌** | 나는 이유를 아니까 이상하지 않았다 |
| 안내 문구가 버튼보다 한참 아래 떠서 못 봄 | 나는 어디 뜨는지 알고 찾아봤다 |
| 어드민에 「오늘 접수」 건수가 없음 | 발주처가 호소한 문제인데 화면 만들다 빠뜨렸다 |
| 휴대폰에서 글자가 밀리고 겹침 | PC로만 확인했다 |

**"만든 사람은 자기 코드의 오류를 못 본다"** 를 몸으로 겪었다.

---

**원본 데이터를 건드리지 않는 법을 배웠다.**

수업에서 들은 원칙을 실제 설계로 옮겨본 게 오늘이 처음이다. 세 번 적용했다.

| 상황 | 처음 생각 | 실제로 한 것 |
|---|---|---|
| 자격증 표기가 흔들림 | check 제약으로 8종만 허용 | 원본 칸은 무제약, `certificate_std` 판정 칸을 따로 |
| 연락처에 하이픈이 섞임 | 저장할 때 정리해서 넣기 | 입력 그대로 저장, `phone_digits` 를 DB가 자동 계산 |
| 담당자가 빈칸을 채워야 함 | 수정 권한을 열기 | `staff_` 칸을 새로 만들고 **원본 칸은 트리거로 잠금** |

마지막 것은 검증까지 했다. 담당자 칸 채우기는 200, 이름 고치기는 **400 「이름은 고칠 수 없습니다」**.
권한을 열어도 신청자가 낸 값은 아무도 못 바꾼다.

세 기관 CSV 1,000행을 합칠 때도 같은 방식이었다. 원본 5칸 × 1,000행을 원본 파일과 하나씩 대조해 **다른 곳 0군데**, 금액 합계 20,339,400원 일치를 확인했다.

---

**"모르면 모른다"를 코드로 만드는 법.**

발주처가 "가장 중요하다"고 한 항목이라 구조부터 그렇게 짰다.

```
질문 → 실기인가? → 확인 못 한 8가지인가? → 문서 검색 → 점수 미달?
                                                    ↓ 미달이면
                                          Gemini 를 아예 부르지 않음
```

**근거를 못 찾으면 AI를 호출하지 않는다.** 그래야 지어낼 수가 없다.
"프롬프트에 모르면 모른다고 하라"고 적는 것과, **구조적으로 부를 수 없게 만드는 것**은 다르다.

---

**한국어는 조사를 떼야 검색이 된다.**

「접수비 얼마예요?」에 답을 못 했다. 비슷한말 사전에 `접수비 → 응시료` 를 넣었는데도.
원인은 **`접수비가` 와 `접수비` 를 다른 낱말로 셌기** 때문이었다.
조사 떼기를 넣고 제목에 가중치를 두니, 답해야 할 질문은 최저 2점 / 거절해야 할 질문은 최고 0점으로 뚜렷하게 갈렸다. **기준값(threshold)을 2로 정한 근거**가 그 숫자다.

---

**"고칠 수 있게 만드는 것"까지가 요구사항이다.**

발주서에 이렇게 적혀 있었다.

> 저희가 문서를 직접 고칠 수 있는지가 중요합니다. 지금은 개발자에게 부탁해야 합니다.

처음엔 문서를 파일(`faq.json`)로 뒀는데, 그러면 **직원이 못 고친다.**
Supabase로 옮기고 어드민 화면을 만들자, 문서를 고치면 챗봇 답이 즉시 바뀌었다 (14,500 → 99,999 로 검증).

거기서 한 걸음 더 갔다. 거절 문구("모르겠습니다...")도 코드에 박혀 있어서 **그것만 여전히 개발자한테 부탁해야 하는 상태**였다.
그래서 **"언제 거절할지"는 코드, "뭐라고 말할지"는 문서**로 나눴다. 인수검사는 안전하게 지키면서 직원이 문구를 고칠 수 있다.

---

## Lacked (부족했던 것)

**그라디오는 파이썬 서버라 Vercel에 못 올라간다는 이야기를 살짝 흘려들었다.**

그래서 챗봇 배포 방법을 정할 때 혼란이 있었다.

- 사이트 안에 넣기 vs Hugging Face vs `share=True`
- 셋을 비교하느라 시간이 걸렸다

결국 **Gradio로 먼저 만들어 `--test` 5문항을 통과시킨 뒤, 같은 규칙을 웹으로 옮겨 사이트 안에 넣는** 방식으로 갔다.
실습가이드 E카드의 확인 방법(`python app.py --test`)은 Gradio판으로 충족했고, 공개 주소는 웹판으로 얻었다.

결과적으로는 두 판 다 남았지만, **처음에 배포 제약을 알고 시작했으면 한 번에 갔을 것이다.**
"이 도구가 어디까지 올라가는가"를 만들기 전에 확인해야 한다.

**그리고 문서를 처음부터 끝까지 안 읽고 시작했다.**

- 발주 문서가 PDF와 md 두 벌이었고 **내용이 달랐다.** md에만 "필기까지, 실기 제외" 제약과 인수검사 7번째 항목이 있었다. PDF로 만들었으면 통째로 놓쳤다.
- QA 폴더가 프로젝트 안에 있는 걸 뒤늦게 알았다. 미리 봤으면 S3·S4를 만들 때부터 반영했을 것이다.

---

## Longed for (바라는 것)

**우선순위에 맞게 작업을 결정하고, 너무 깊은 수정에 빠지지 않게 컨트롤이 필요하다.**

오늘 여러 번 이랬다.

- 화면 색·여백·글자 크기를 고치다가 **한 시간 가까이** 쓴 구간이 있었다
- 강사님이 "FAQ보다 DB 통합이 1순위"라고 짚어주기 전까지 순서를 잘못 잡고 있었다
- 접수 내역을 통합하다가 QA 전에 고치기로 한 S3·S4를 놓쳤다

**필요한 기준**

| 물어볼 것 | 왜 |
|---|---|
| 이건 **인수검사 항목**인가, 아니면 내 눈에 거슬리는 건가? | 인수검사가 완료 판정 기준이다 |
| 지금 안 고치면 **뒤가 막히는가**? | 막히는 것부터. 색은 나중에도 고친다 |
| **몇 분**짜리인가? | 10분이라고 생각한 게 40분이 되면 멈추고 다시 판단 |

**작게 쪼개서 확인받는 흐름은 잘 잡혔다.**
기획 → 컨펌 → 실행 → 확인을 반복하니 방향이 틀어져도 되돌릴 양이 적었다. 이건 유지한다.

**QA를 만들면서 하는 것도 해보고 싶다.**
오늘은 다 만들고 나서 QA를 받았는데, 체크리스트를 먼저 보고 만들었으면 이름 검사·연락처 검사를 처음부터 넣었을 것이다.

---

## 핵심 교훈

> **만든 사람은 자기 코드의 오류를 못 본다.**
> 나는 늘 `김두두 / 01012345678` 만 넣어봤다. `ㅎ`, `12345`, `010` 은 넣어볼 생각을 안 했다. 정해진 체크리스트가 그걸 대신 물어봐 줬다.

> **원본을 고치지 않고도 정리할 수 있다.**
> 원본 칸은 그대로 두고 판정 칸을 따로 만들면 된다. 판정이 틀리면 판정만 고치면 되고, 원본은 언제나 되돌릴 수 있다.

> **"모르면 모른다"는 프롬프트가 아니라 구조로 만든다.**
> 근거를 못 찾으면 AI를 호출하지 않으면 된다. 부를 수 없으면 지어낼 수 없다.

> **고칠 수 있게 만드는 것까지가 요구사항이다.**
> 발주처가 원한 건 "정확한 답"이 아니라 "우리가 고칠 수 있는 답"이었다. 문서를 파일에 두면 그 요구를 절반만 채운 것이다.

> **도구의 배포 제약은 만들기 전에 확인한다.**
> Gradio가 Vercel에 안 올라간다는 걸 먼저 알았으면 한 번에 갔다.

---

**작성 완료**: 2026-08-19
**상태**: 1일차 목표 전부 달성 / QA 1회차 FAIL 7건 당일 수정 완료
**다음 세션(8/20)**: QA 재테스트 → 어르신 말 튜닝 → 통합 테스트 3종 → 사용성 평가 → 수행일지 PDF 제출
