# 미니 프로젝트 1 수행일지

이름: iny    선택한 자격증: 한식조리기능사

> **제출**: MLP AI 스튜디오 > 과제 제출 > **미니프로젝트(1)** 에 PDF로 올립니다.
> **분량**: 1~2쪽. 길게 쓰지 않아도 됩니다.
> 아래 칸만 채우면 분량이 맞습니다.

---

## 8/19 (수)

**오늘 목표**

내용 확인 후 기본 뼈대 만들기 / 데이터 접수 및 확인 점검 / FAQ 내용 세팅

**한 일** (한 줄씩)
- 오전: 발주 문서 3종 확인 → 프로젝트 인덱스(INDEX.md) 작성 → GitHub 저장소 생성·연동 → Supabase 접수 테이블 SQL 설계
- 오후: 접수 화면·DB 연결 완성 → 접수 어드민 → GitHub·Vercel 배포 → 비밀키 점검
- 오후: DB 통합(1,000행) → FAQ 챗봇 → 문서를 Supabase로 옮겨 어드민에서 수정 가능하게 → 챗봇을 사이트 안으로 → 접수 내역 하나로 통합 → QA 테스트·수정

**막힌 것과 푼 방법** - 한 건 이상 씁니다

### 1. 자격증 표기 흔들림을 DB에서 막으려다 원본을 훼손할 뻔함

| | |
|---|---|
| 무엇을 하려 했나 | 워드 원장에 같은 자격증이 `한식`, `한식조리`, `한식조리기능사`로 섞여 적혀 있어서, DB `certificate` 컬럼에 8종만 허용하는 check 제약을 걸어 표기를 통일하려 했다 |
| 화면에 나온 말 | 오류 메시지 없음. SQL을 실행하기 전 설계 검토 단계에서 발견 |
| 시도한 것 | 수업에서 배운 원본 데이터 처리 원칙에 걸렸다. check 제약을 걸면 원장의 `한식` 같은 행이 DB에 **아예 못 들어오고**, 옮기면서 값을 고치면 **원본이 변형된다**. 제약을 전부 빼고 원본 컬럼(name/phone/certificate/channel/note)을 무제약으로 두는 대신, 판정용 파생 컬럼 3개를 추가했다 — `certificate_std`(표준명 판정), `phone_digits`(숫자만 추출, 자동 계산), `review_needed`(확인 필요 행 표시, 자동 계산) |
| 결과 | 원본은 들어온 그대로 저장되고, 집계·조회는 파생 컬럼을 기준으로 한다. 판정이 안 되면 `null`로 남아 "확인 필요"로 드러난다. **원본을 지우거나 고치지 않고도** 표기 흔들림을 처리하게 됐다 |

### 2. 발주 문서 PDF와 md의 내용이 달랐음

| | |
|---|---|
| 무엇을 하려 했나 | 발주 문서를 읽고 접수 화면에 표시할 자격증 정보(응시료 등)를 정하려 했다 |
| 화면에 나온 말 | 오류 없음. 문서 두 벌을 대조하다 발견 |
| 시도한 것 | md에만 **"이번 범위는 필기 접수까지. 실기 제외"** 제약과 "실기를 물으면 필기만 안내한다"는 거절 규칙이 있었다. 인수검사도 PDF 6개 → md 7개, 수수료표도 PDF는 필기+실기 / md는 필기만, 기관명도 전부 가상명으로 바뀌어 있었다. md를 정본으로 확정하고 차이를 INDEX.md 10절에 기록했다 |
| 결과 | 접수 화면에서 실기 응시료를 빼고, 챗봇 요구사항에 "실기 질문 거절"을 추가했다. PDF 기준으로 만들었다면 인수검사 3번을 통째로 놓쳤을 것 |

### 3. GitHub 활성 계정이 지정 계정이 아니었음

| | |
|---|---|
| 무엇을 하려 했나 | GitHub 저장소를 만들어 로컬 저장소와 연동하려 했다 |
| 화면에 나온 말 | `gh auth status` 결과에 `✓ Logged in to github.com account wkddlsdud971014-young` / `Active account: true` |
| 시도한 것 | 쓰기로 한 계정(iny101400)이 아니라 다른 계정이 활성 상태였다. 그대로 만들었으면 저장소와 이후 Vercel 배포까지 전부 다른 계정에 묶였을 것이다. `gh auth switch --user iny101400-young` 실행 후 `gh api user --jq .login`으로 확인했다 |
| 결과 | `iny101400-young/iny-senior-cert-apply` 로 생성됐고 `gh repo view`에서 owner를 눈으로 확인했다 |

### 4. 수업 자료를 개인 계정에 올림 — 계정을 통째로 옮김

| | |
|---|---|
| 무엇을 하려 했나 | 3번에서 계정을 확인해 `iny101400`으로 저장소를 만들었는데, 이 프로젝트는 **수업 자료라 `wkddlsdud971014` 계정에 올려야** 하는 것이었다. 처음에 반대로 알고 있었다 |
| 화면에 나온 말 | 오류 없음. `gh repo view` 결과의 owner가 `iny101400-young`으로 나온 것을 보고 알아챘다 |
| 시도한 것 | 저장소를 지우기 전에 **먼저 옮겼다.** 로컬 git에 커밋 4개가 그대로 있으므로 `git remote remove origin` → `gh auth switch --user wkddlsdud971014-young` → `gh repo create ... --source=. --push` 순으로 새 계정에 올린 뒤, 원래 저장소를 삭제했다. 지우고 다시 만들면 그 사이에 원본이 없는 구간이 생긴다 |
| 결과 | `wkddlsdud971014-young/iny-senior-cert-apply`에 커밋 4개가 그대로 올라갔다. **계정과 이름은 별개**라는 것도 정리됐다 — 계정은 `wkddlsdud971014`, 저장소 이름은 강사 요청대로 닉네임 `iny`를 넣는다. Supabase·Vercel은 아직 만들기 전이라 손댈 것이 없었다 |

### 5. 어드민의 접수 시각이 9시간 어긋남

| | |
|---|---|
| 무엇을 하려 했나 | 어드민에서 "오늘 몇 건 접수됐는지"를 세려고 했다. 발주처가 지금은 눈으로 세다가 자주 틀린다고 한 항목이다 |
| 화면에 나온 말 | 접수 완료 화면은 `11시 11분`인데 Supabase 표에는 `02:11:12` |
| 시도한 것 | 틀린 값이 아니라 Supabase가 시각을 **세계표준시(UTC)** 로 저장하기 때문이었다. 한국은 UTC보다 9시간 빠르다. 그대로 두면 **새벽 9시 이전에 들어온 접수가 전날 것으로 잡혀서** "오늘 몇 건"이 틀린다. 어드민에서 `Asia/Seoul` 기준으로 바꿔 보여주도록 했다 |
| 결과 | 어드민 표에 `2026-08-19 11:11`로 나온다. 오늘 건수도 한국 날짜 기준으로 센다 |

### 6. 연락처 하이픈 — 원본은 두고 보이는 것만 바꿈

| | |
|---|---|
| 무엇을 하려 했나 | 원장에 `010-2345-6789`와 `01034567890`이 섞여 있어서, 어드민에 원본과 "숫자만 뽑은 값"을 같이 보여줬다 |
| 화면에 나온 말 | 오류 없음. 화면을 보니 한 칸에 같은 번호가 두 줄로 나와 지저분했다 |
| 시도한 것 | 두 줄로 보여줄 필요가 없다고 판단해, 숫자만 넣어도 하이픈이 자동으로 붙게 바꿨다. **저장은 입력한 그대로 두고 화면에서만** 하이픈을 붙였다. 전화번호 자릿수(9~11)가 아니면 손대지 않고 원본을 그대로 보여준다 |
| 결과 | 접수 화면은 입력하는 동안 `010-9876-5432`로 바뀌고, 어드민도 같은 모양으로 나온다. DB의 `phone` 칸에는 여전히 입력 원본이 들어 있다. **원본을 안 고치고도 보기 좋게** 만드는 방법을 알게 됐다 |

### 7. 배포하면 클라이언트 문서가 인터넷에 공개될 뻔함

| | |
|---|---|
| 무엇을 하려 했나 | GitHub 저장소를 그대로 Vercel에 올리려 했다 |
| 화면에 나온 말 | 오류 없음. 배포 전에 폴더 구조를 보다가 발견 |
| 시도한 것 | 저장소 루트를 그대로 배포하면 `접수대장.docx`(이름·연락처 22줄)와 "사내 원장"이라 적힌 `02_안내규정.md`가 주소만 알면 열린다. 저장소를 Private으로 해도 **Vercel 배포는 공개**라 소용이 없다. Vercel의 **Root Directory를 `starter`로** 지정해 배포 범위를 좁혔다 |
| 결과 | `/client-docs/` 로 접속하면 **404**가 뜨는 것을 확인했다. 접수 화면(`/`)과 어드민(`/admin`)만 열린다 |

### 8. 파일을 직접 열면 `/admin/` 링크가 폴더 목록으로 뜸

| | |
|---|---|
| 무엇을 하려 했나 | 접수 화면 우측 상단에 어드민으로 가는 링크를 달았다 |
| 화면에 나온 말 | 어드민 화면 대신 `[상위 디렉터리]` `index.html 11.2 kB` 같은 **파일 목록**이 떴다 |
| 시도한 것 | 파일을 직접 열면(`file://`) 브라우저가 폴더 주소에서 `index.html`을 자동으로 찾지 못한다. 링크를 `admin/`에서 `admin/index.html`로 바꿔 파일까지 명시했다 |
| 결과 | 파일로 열어도, Vercel에 올려도 둘 다 정상 동작한다. 배포 후 `/admin/`도 200으로 열리는 것을 확인했다 |

### 9. 세 기관 CSV를 한 표로 합치기

| | |
|---|---|
| 무엇을 하려 했나 | 두두넷 국가기술·전문·두두보건 세 곳의 접수 데이터 1,000행을 한 표로 합치려 했다 |
| 화면에 나온 말 | 오류 없음. 파일을 열어 보니 필드명이 한글 / `applicant_name` / `fullName` 로 제각각이었다 |
| 시도한 것 | 표기가 다른 것이 성별(`남·여` / `M·F` / `1·2`), 결제상태(`완료` / `PAID` / `success`), 날짜(`2026-09-05` / `2026/10/18` / **`13-10-2026`**) 등 여러 개였다. 두두보건 날짜는 **일-월-년 순**이라 그대로 읽으면 전부 틀린다. 원본 칸은 그대로 두고 표준 칸을 따로 만들어 채웠다 |
| 결과 | 1,000행 46칸. **원본 5칸 × 1,000행을 원본 파일과 하나씩 대조**해 다른 곳 0군데, 금액 합계 20,339,400원 일치를 확인했다. 빈 칸 4종(시험지역 300 / 회차 906 / 차수 800 / 교육기관 700)도 전부 이유가 설명된다 |

### 10. Gemini 모델이 신규 사용자에게 중단됨

| | |
|---|---|
| 무엇을 하려 했나 | 챗봇이 찾은 근거로 자연스러운 문장을 만들게 하려 했다 |
| 화면에 나온 말 | `HTTP Error 404` → `This model models/gemini-2.5-flash is no longer available to new users` |
| 시도한 것 | 쓸 수 있는 모델 목록을 불러와 확인했다. 다음 모델로 바꾸니 이번에는 답이 `한식조리기능사 필기 응시` 처럼 중간에 잘렸다. **요즘 모델은 답하기 전에 "생각"하는 데도 토큰을 써서** 정작 답할 자리가 모자란 것이었다. 답 길이를 1024 → 2048로 늘려 해결 |
| 결과 | `gemini-3.5-flash-lite` 로 고정. 채점 5문항 전부 통과 |

### 11. "접수비 얼마예요?" 에 답하지 못함

| | |
|---|---|
| 무엇을 하려 했나 | 어르신이 쓰시는 말로 물어도 답이 나오게 하려 했다 (발주서 91줄) |
| 화면에 나온 말 | "모르겠습니다. 저희가 확인하지 못한 내용이라..." |
| 시도한 것 | 비슷한말 사전에 `접수비 → 응시료` 를 넣었는데도 못 찾았다. 이유는 **`접수비가` 와 `접수비` 를 다른 낱말로 셌기** 때문이었다. 한국어는 낱말 뒤에 조사가 붙는데 그걸 안 뗐다. **조사 떼기**를 넣고, 본문보다 **제목에 겹치는 낱말을 두 배**로 세게 했다 |
| 결과 | 답해야 할 질문은 최저 2점, 거절해야 할 질문은 최고 0점으로 뚜렷하게 갈렸다. 그래서 기준값(threshold)을 **2**로 정했다. 「환불 되나요?」도 이제 「환불 규정」 문서를 1등으로 잡는다 |

### 12. `.vercelignore` 에 파일 이름만 적었더니 함수까지 사라짐

| | |
|---|---|
| 무엇을 하려 했나 | 강사님 점검 항목 4번대로 Supabase 접속 정보를 Vercel 환경변수로 옮기려 했다 |
| 화면에 나온 말 | `/api/config` 가 **404 NOT_FOUND** |
| 시도한 것 | 정적 파일은 환경변수를 못 읽어서, 서버 함수(`api/config.js`)가 환경변수를 읽어 내려주게 만들었다. 그런데 로컬 `config.js` 가 배포에 딸려 올라가 함수가 안 불렸다. `.vercelignore` 에 `config.js` 라고 적었더니 **이름이 같은 `api/config.js` 까지 함께 빠져** 함수 자체가 사라졌다 |
| 결과 | 맨 앞에 `/` 를 붙여 `/config.js` 로 적으니 루트 파일만 빠졌다. `/config.js` 를 열면 이제 주석 없이 값 두 줄만 나온다 — 파일이 아니라 환경변수에서 만들어졌다는 뜻이다 |

### 13. 담당자가 빈칸을 채우게 하려니 원본까지 고칠 수 있게 됨

| | |
|---|---|
| 무엇을 하려 했나 | 우리 접수는 7칸만 받아서 통합 표와 나란히 놓으면 성별·시험장이 비어 있다. 담당자가 전화로 확인해 채우게 하려 했다 |
| 화면에 나온 말 | 오류 없음. 수정 권한을 열면 신청자가 낸 이름·연락처도 덮어쓸 수 있다는 것을 설계 중에 알아챘다 |
| 시도한 것 | 담당자가 채우는 칸(`staff_` 로 시작하는 10개)을 새로 만들고, **DB 트리거로 원본 칸을 잠갔다.** 이름·연락처·자격증·비고·접수일을 고치려 하면 DB가 거부한다 |
| 결과 | 담당자 칸 채우기 → 200 성공 / 이름 고치기 → **400 「이름은 고칠 수 없습니다」**. 권한을 열어도 원본은 아무도 못 바꾼다 |


**되는지 확인**
- 접수하면 DB에 쌓인다 ( **O** )
- 챗봇이 근거 있는 답을 한다 ( **O** ) — 채점 5문항 통과, 답마다 근거 문서를 함께 표시
- 모르는 질문에는 모른다고 한다 ( **O** ) — 근거를 못 찾으면 Gemini 를 아예 부르지 않음

**고민 포인트** (정답이 없어 판단이 필요했던 것)

| 무엇 | 어떻게 정했나 |
|---|---|
| 지금 접수할 수 없는 자격증(위생사·손해평가사·공인중개사) | 숨기지 않고 **흐리게 두고 이유를 표시**했다. 아예 숨기면 "그 자격증은 안 다루나" 하고 전화하시게 된다 |
| 우리 접수의 접수상태 값 | 통합 데이터의 `접수완료` 는 두두넷 원서접수까지 마친 4단계다. 우리 폼 제출은 2단계라 뜻이 달라 **`접수`** 로 구분했다 (발주서 3절) |
| 거절 문구를 어디에 둘까 | **"언제 거절할지"는 코드, "뭐라고 말할지"는 문서**로 나눴다. 인수검사는 안전하게 지키면서 직원이 문구를 고칠 수 있다 |
| 어드민 비밀번호 | 브라우저에서 도는 검사라 실제 보안이 아니다. "신청자가 실수로 들어가는 것"을 막는 용도이며, 실무라면 로그인이 필요하다는 것을 코드 주석에 남겼다 |

**QA 결과** (16:07 ~ 16:44)

```
Must   8/8 PASS
Should 7/8 PASS   S4 FAIL - 연락처 010만 넣어도 접수됨
Could  3/4 PASS   C1 FAIL - 휴대폰에서 글자 밀림·겹침
시나리오 5/5 완주, 엣지 10/10 기록
```

FAIL 7개를 그날 저녁에 고쳤다. 이름 검사(두 글자 이상·한글영문만·30자 이하), 연락처
자릿수 검사, 안내 문구를 잘못된 칸 바로 아래로, 휴대폰 화면 정리, 어드민에
「오늘 접수」 건수·최신순 정렬·표머리 거르기 추가.

**내일 할 것**

- QA 재테스트 — 고친 것이 실제로 통과하는지 확인하고 「수정 후 PASS」 표기
- 어르신 말 튜닝 — 포크레인 말고 지게차 면허·요양사 등도 되는지 확인
- 챗봇 기준값(threshold) 조정
- 통합 테스트 3종 + 사용성 평가
- 수행일지 PDF 제출

---

## 8/20 (목)

**결과물**

| 항목 | 주소 |
|---|---|
| GitHub | https://github.com/wkddlsdud971014-young/iny-senior-cert-apply |
| 배포 사이트 | https://iny-senior-cert-apply.vercel.app |
| 도전1 (S2 기본형) | https://e89c37ec2ddc7a0752.gradio.live |
| 도전3 앞 (S4 키워드) | https://1acfe3597c073ddb62.gradio.live |
| 도전3 뒤 (S5 TF-IDF) | https://7321fa2f02caedad9a.gradio.live |
| 도전4 (S6 완성형) | https://4592025d91fba324ae.gradio.live |

> gradio.live 주소는 내 컴퓨터에서 프로그램이 돌고 있어야 열립니다. 최대 72시간이며 노트북을 덮으면 끊깁니다. 계속 살아 있는 것은 위의 배포 사이트입니다.

---

**오늘 목표**

FAQ 챗봇 실습 세트(S1~S6)를 도전1~4 순서로 직접 돌려보고, 검색 방식이 결과를 어떻게 바꾸는지 눈으로 확인한다. 확인한 것을 어제 배포한 접수 사이트 챗봇에 반영한다.

**한 일** (한 줄씩)

- 실습 환경 준비 → Gemini 모델 은퇴 문제 해결 → 도전1(S1 로컬 + S2 공유 URL, 휴대폰 접속 확인)
- 도전2(S3 3탭 챗봇) MUST 챌린지 — FAQ 추가·동의어 추가의 전/후 비교 2건
- 도전2 선택과제 — 저장 위치를 파일에서 Supabase 테이블로 전환
- 도전3(S4 4,705건 확장 → S5 TF-IDF 교체) — 같은 질문 4개를 두 단계에 던져 대비 확인
- 도전4(S6 완성형) — 배포본에 없던 동의어 기능을 옮겨 3탭으로 완성
- 실습에서 찾은 것을 실제 서비스에 이식 — 줄임말 7개 추가, 검색을 IDF 가중치로 교체, 배포

---

### 막힌 것과 푼 방법

#### 1. 교재대로 했는데 답변만 404 — 모델이 은퇴돼 있었음

| | |
|---|---|
| 무엇을 하려 했나 | S1 챗봇을 처음 띄워 "한식조리기능사 시험비가 얼마예요?"를 물었다 |
| 화면에 나온 말 | `[ERROR] HTTPError: HTTP Error 404: Not Found`. 검색은 정상이고 답변 단계에서만 났다 |
| 시도한 것 | 키가 잘못됐는지 모델이 문제인지 갈라야 했다. 같은 키로 모델 목록을 조회해 보니 37개가 정상적으로 왔다. 키는 멀쩡하다는 뜻이다. 그다음 에러 본문을 열어 보니 답이 적혀 있었다 — `This model models/gemini-2.5-flash-lite is no longer available to new users. Please update your code to use models/gemini-3.5-flash-lite`. 교재가 만들어진 뒤 구글이 이 모델을 신규 사용자에게 닫은 것이다 |
| 결과 | `gemini-3.5-flash-lite`로 교체했다. `.env` 한 곳만 고치면 `gemini.py` 기본값과 `.env.example`에 예전 값이 남아 언제든 되돌아간다. 특히 `.env.example`은 "이걸 복사해서 .env를 만들라"는 원본이라 안 고치면 다음 복사 때 부활한다. 단계마다 4곳(코드 기본값·주석·`.env`·`.env.example`) × 6단계 = 24곳을 전부 바꿨다. HOWTO.md 2곳에도 변경 사유와 날짜를 적었다 |

**알게 된 것**: 에러 메시지 본문에 해결 방법이 그대로 적혀 있는 경우가 많다. 그리고 설정값은 한 곳만 고치면 안 되고, 그 값이 박혀 있는 곳을 전부 찾아야 한다.

#### 2. 작업 도중 실습 폴더가 통째로 교체돼 수정이 사라짐

| | |
|---|---|
| 무엇을 하려 했나 | 1번 조치를 끝내고 S2 공유 URL을 띄우려 했다 |
| 화면에 나온 말 | 오류 없음. 파일을 다시 확인하다 예전 모델명이 되살아나 있는 것을 발견했다 |
| 시도한 것 | 강사님 자료가 갱신되면서 `chatbot_build` 폴더가 새 버전으로 덮어써졌다. 내가 고친 26곳과 직접 만든 `.env` 6개가 전부 사라졌다. `.env`는 원래 배포물에 없는 파일이라 같이 지워졌다. 무엇을 왜 고쳤는지 기록해 둔 덕에 같은 작업을 다시 적용했다. 새 버전에서 `stage3/.env.example`은 이미 `gemini-3.5-flash-lite`로 바뀌어 있었다 — 강사님도 같은 문제를 발견하고 고치는 중이었다는 뜻이라 내 판단이 맞았음을 확인했다 |
| 결과 | 3분 만에 복구했다. API 키 원본이 `chatbot/.env`에 따로 있어서 키를 다시 발급받을 필요는 없었다. 새 버전에는 `store.py`·`schema.sql`·`seed.py`가 추가돼 있어 선택 과제(Supabase 전환)를 할 수 있게 됐다 |

**알게 된 것**: 남이 주는 자료 위에서 작업할 때는 내 수정사항이 어디에 있는지 알고 있어야 한다. 기록이 곧 복구 속도다.

#### 3. 허깅페이스에 올리려다 결제 요구를 받음

| | |
|---|---|
| 무엇을 하려 했나 | gradio.live 주소는 72시간이면 죽으니, 계속 살아 있는 주소를 만들려고 허깅페이스 Spaces에 올리려 했다 |
| 화면에 나온 말 | `402 Client Error: Payment Required` / `Static Spaces are free for everyone, but hosting Gradio and Docker Spaces on free cpu-basic requires a PRO subscription` |
| 시도한 것 | 로그인은 정상이었고(계정·Write 권한 확인) 딱 저장소를 만드는 단계에서 막혔다. Gradio Space가 유료로 바뀐 것이다. 무료로 남은 Static Space는 HTML만 되고 파이썬 서버를 못 돌려 챗봇에는 쓸 수 없다. 교재 기본 방식인 `share=True`로 방향을 돌렸다 |
| 결과 | `share=True`로 공유 URL을 만들었다. 그리고 이 일이 뒤에 도움이 됐다 — "그럼 Gradio를 홈페이지에 심으면 되지 않나"를 검토할 때, 그 링크가 노트북이 켜져 있어야만 사는 것임을 이미 알고 있었기 때문에 다른 길(서비스 챗봇 자체를 개선하는 쪽)을 택할 수 있었다 |

**알게 된 것**: "무료"는 시점의 정보다. 배포 방법을 정할 때는 "무료인가"가 아니라 "지금도 무료인가"를 확인해야 한다.

#### 4. SQL 실행 오류 — 컬럼 이름이 예약어였음

| | |
|---|---|
| 무엇을 하려 했나 | 선택 과제로 FAQ 저장 위치를 Supabase 테이블로 옮기려고 `schema.sql`을 SQL Editor에 붙여넣고 실행했다 |
| 화면에 나온 말 | `ERROR: 42601: syntax error at or near "full"` / `LINE 11: full text not null` |
| 시도한 것 | `full`은 Postgres가 이미 쓰고 있는 예약어다(`FULL OUTER JOIN` 등). 컬럼 이름으로 쓰려면 큰따옴표로 감싸야 한다. `"full" text not null`로 고쳤다. 따옴표로 감싸도 컬럼 이름 자체는 `full` 그대로라 이 컬럼을 읽는 `store.py`(`r["full"]`)는 수정할 필요가 없었다 |
| 결과 | `Success. No rows returned`. `faq`·`synonyms` 두 테이블이 생겼다 |

#### 5. Supabase를 넣어도 켜지지 않음 — 에러도 안 남

| | |
|---|---|
| 무엇을 하려 했나 | 테이블을 만들고 `.env`에 접속 정보를 넣은 뒤 챗봇을 재시작했다 |
| 화면에 나온 말 | **오류 없음**. 화면도 똑같고 답변도 정상이었다. 그런데 `faq.json`을 숨기고 실행하니 파일이 없다며 죽었다 — 테이블이 아니라 파일을 읽고 있었다는 뜻이다 |
| 시도한 것 | `app.py`의 순서를 봤다. 19줄에서 `from rag import ...`을 하고 35줄에서 `load_env()`를 부른다. `rag`는 불려가면서 `store`를 부르고, `store`는 그 순간 환경변수에서 `SUPABASE_URL`을 읽어 저장 위치를 정한다. 즉 **`.env`를 읽기 전에 "설정이 있느냐"를 물어본 것**이라 항상 "없음"으로 판정돼 파일 모드로 떨어졌다. `load_env()`를 import보다 위로 옮기고, 왜 그래야 하는지 주석을 달았다 |
| 결과 | `faq.json`과 `synonyms.json`을 아예 숨긴 채로 실행해도 101건이 정상 응답했다. 저장 위치가 테이블로 바뀐 것을 확인했다 |

**알게 된 것**: 증상이 안 보이는 버그가 제일 나쁘다. 에러도 없고 화면도 같아서, 모르고 지나가면 "분명 넣었는데 왜 안 쌓이지" 하며 오래 헤맬 수 있다. 그리고 파이썬은 `import` 하는 순간 그 파일이 실행되므로, 설정을 읽는 순서가 중요하다.

#### 6. FAQ를 추가했는데 챗봇이 여전히 모른다고 함 — 순위에서 밀렸다

| | |
|---|---|
| 무엇을 하려 했나 | 도전2 MUST 챌린지. "한식조리기능사 실기 위생복 안 입으면 어떻게 되나요?"가 `[UNKNOWN]`인 것을 확인하고, FAQ 관리 탭에서 실기 복장 규정 FAQ를 추가한 뒤 같은 질문을 다시 했다 |
| 화면에 나온 말 | 추가한 뒤에도 `[UNKNOWN] 제공된 FAQ에서 확인할 수 없는 내용입니다` |
| 시도한 것 | 저장이 안 된 건지 못 찾는 건지부터 갈랐다. `faq.json`을 열어 보니 `CUSTOM_101`로 정상 저장돼 있었다. 그러면 검색 문제다. 점수를 직접 계산해 보니 — 기존 FAQ "한식조리 실기 준비물"이 8점, 내가 넣은 것이 5점이었다. `rag.py`의 `answer_question()`은 **1등 하나만** Gemini에게 넘긴다. Gemini가 받은 근거에는 "안 입으면 어떻게 되는지"가 없어서 지어내지 않고 `UNKNOWN`이라고 답한 것이다. 정답을 가진 문서는 2등이라 전달조차 되지 않았다. 점수 차이의 원인은 키워드 매칭이 `if 키워드 in 질문`으로 **글자가 들어있는지**를 보기 때문이다. 기존 FAQ가 가진 `한식`이라는 짧은 키워드가 질문의 `한식조리기능사` 안에 들어 있어 점수를 챙겼다 |
| 결과 | 기존 FAQ와 겹치지 않는 질문("한식조리 위생모 안 쓰면 실격되나요?")으로 바꾸니 `[UNKNOWN]` → `[ANSWERED]`로 정상 동작했다. 원래 질문은 고치지 않고 한계 사례로 남겼다. 오후(도전3)에서 이 문제가 다시 나온다 |

**알게 된 것**: 키워드 매칭은 "내용이 정확한 문서"가 아니라 **"흔한 단어를 많이 가진 문서"** 를 위로 올린다. 교재는 이 한계를 4,705건에서 겪게 설계했는데 101건에서 이미 나타났다.

#### 7. 실습에서 찾은 줄임말을 사이트에 옮겼는데 하나만 안 됨

| | |
|---|---|
| 무엇을 하려 했나 | 실습에서 찾은 줄임말(요보사·손평사·개사 등)을 어제 배포한 사이트 챗봇에 추가하고 배포했다 |
| 화면에 나온 말 | 배포 후 실제 사이트에 물으니 `요보사`·`손평사`는 정상인데 `개사 환불 규정 알려주세요`만 "모르겠습니다" |
| 시도한 것 | 먼저 배포가 실제로 반영됐는지부터 확인했다. 이번에 새로 넣은 `포클레인`(ㅋ)으로 물으니 정상 응답 — 배포는 됐다는 뜻이다. 그다음 `chat.js`를 로컬에서 돌리는 장치를 만들어 토큰을 찍어 봤다. `개사` 질문의 토큰에도 `공인중개사`가 들어 있었다 — 검색은 제대로 되고 있었다. 코드를 따라가 보니 `tokens(question)`은 동의어를 확장하지만 `askGemini(question, …)`에는 **원래 질문이 그대로** 간다. 근거 문서는 제대로 찾아 놓고도, 문장을 다듬는 Gemini가 "개사"를 못 알아들어 거절한 것이다. `요보사`·`포클레인`은 Gemini가 문맥으로 눈치챘지만 `개사`는 너무 짧았다 |
| 결과 | 자격증 줄임말 전용 표(`CERT_ALIASES`)와 `aliasHint()`를 만들어 프롬프트 끝에 `[줄임말 풀이] 개사 = 공인중개사` 한 줄을 붙였다. 질문 문장 자체는 고치지 않았다 — 기존 동의어 표에는 `얼마 → 응시료` 같은 항목도 있어서 그대로 치환하면 "얼마예요"가 "응시료예요"가 되어 문장이 망가진다. 정식 명칭을 이미 쓴 질문에는 풀이를 붙이지 않게 했다 |

**알게 된 것**: 검색이 되는 것과 답이 나오는 것은 별개다. 어느 단계에서 막혔는지를 먼저 갈라야 한다.

#### 8. GitHub에 올렸는데 사이트가 그대로 — 자동 배포가 아니었음

| | |
|---|---|
| 무엇을 하려 했나 | 줄임말과 IDF 개선을 커밋·push하고, 실제 사이트에서 반영을 확인하려 했다 |
| 화면에 나온 말 | push는 성공(`b9153bd..195755f main -> main`)했는데 사이트 응답은 예전 그대로였다. "환불 언제까지 되나요"가 계속 상시 종목 접수 문서를 근거로 잡았다 |
| 시도한 것 | 처음에는 "배포 지연이겠지" 하고 두 번 더 기다렸다가 다시 확인했는데도 같았다. `vercel ls`로 배포 목록을 보니 **최신 배포가 20시간 전**이었다. 오늘 push한 세 커밋 중 하나도 배포되지 않은 것이다. `.vercel` 링크 파일도 없었다. 이 프로젝트는 GitHub 연동 자동 배포가 아니라 **CLI로 직접 올리는 방식**이었다. `starter` 폴더에서 `vercel link --project iny-senior-cert-apply` 로 기존 프로젝트에 연결한 뒤 `vercel --prod` 로 올렸다 |
| 결과 | 배포 후 확인하니 "환불 언제까지 되나요" → 환불 규정, "가상계좌 언제까지 입금해요" → 가상계좌 입금 기한, "개사 환불 규정" → 환불 규정으로 모두 정상 동작했다. 거절 규칙(날씨·실기·주차)도 그대로였다 |

**알게 된 것**: `git push`와 `배포`는 다른 일이다. 저장소에 올라간 것이 곧 사이트에 반영된 것은 아니다. 프로젝트마다 배포 방식이 다르므로 확인해야 한다.

#### 9. "배포됐다"고 두 번 잘못 판단함 — 확인용 질문을 잘못 골랐다

| | |
|---|---|
| 무엇을 하려 했나 | 8번의 배포가 반영됐는지 확인하려고 새로 추가한 줄임말로 질문했다 |
| 화면에 나온 말 | `포클레인 접수비 얼마예요?` → "굴착기운전기능사의 필기 응시료는 14,500원입니다" (정답) |
| 시도한 것 | 이 답을 보고 "새 동의어가 먹었으니 배포가 됐다"고 판단했다. 틀린 판단이었다. 이 질문은 `접수비 → 응시료`(원래부터 있던 동의어)만으로도 "기능사 필기 응시료" 문서를 찾을 수 있고, 그 문서에는 "기능사는 종목이 달라도 14,500원"이라고 적혀 있다. 즉 **`포클레인`을 못 알아들어도 정답이 나오는 질문**이었다 |
| 결과 | 배포 여부는 결국 `vercel ls`로 배포 시각을 직접 확인해서 밝혔다. 이후로는 "그 변경이 없으면 절대 못 맞히는 질문"으로만 확인했다 — 예: "환불 언제까지 되나요"는 IDF 없이는 환불 문서가 상위 3개 안에 들지 않으므로 답이 나올 수 없다 |

**알게 된 것**: 확인용 질문은 **다른 경로로는 정답이 나올 수 없는 것**이어야 한다. 그렇지 않으면 바뀌지 않았는데도 바뀐 것처럼 보인다. 이것은 도전2에서 전/후 비교를 설계할 때 신경 썼던 것과 같은 문제인데(기존 FAQ가 이겨서 비교가 성립하지 않았던 일), 정작 배포 확인에서는 놓쳤다.

---

### 테스트 수행 일지

**1) 도전2 MUST — 동의어 추가 전/후**

- 수행: 챗봇 탭에서 "굴삭기 응시료 얼마예요?" 질문 → 동의어 관리 탭에서 `굴삭기 → 굴착기` 추가 → 같은 질문 재실행
- 기대: 추가 전 `[UNKNOWN]`, 추가 후 굴착기 FAQ가 출처로 표시
- 실제: 추가 전 `[UNKNOWN] 출처: 없음`, 추가 후 `[ANSWERED] 굴착기운전기능사 필기시험 응시료는 14,500원… 출처: 굴착기운전기능사 - 굴착기운전기능사 응시료`
- 조치: 없음
- 재실행: 같은 결과
- 판정: PASS
- 비고: 이 답은 **원래 FAQ에 있던 내용**이다. 챗봇이 "굴삭기"라는 말을 못 알아들어 못 찾았을 뿐이다. 아는 것이 늘어난 것이 아니라 찾는 능력이 늘어난 사례다.

**2) 도전2 MUST — FAQ 추가 전/후**

- 수행: "한식조리 위생모 안 쓰면 실격되나요?" 질문 → FAQ 관리 탭에서 실기 복장 규정 FAQ 추가 → 같은 질문 재실행
- 기대: 추가 전 `[UNKNOWN]`, 추가 후 내가 넣은 FAQ가 출처
- 실제: 추가 전 `[UNKNOWN] 출처: 없음`, 추가 후 `[ANSWERED] 네, 위생모를 미착용 시 실격 처리됩니다… 출처: 한식조리기능사 - 한식조리기능사 실기 복장 규정`
- 조치: 처음 쓴 질문("실기 위생복 안 입으면")은 추가 후에도 `[UNKNOWN]`이었다. 기존 FAQ가 순위에서 이겨 정답 문서가 Gemini에 전달되지 않았기 때문이다(막힌 것 6번). 기존 FAQ와 겹치지 않는 질문으로 바꿔 다시 측정했다
- 재실행: 삭제 → 질문(`UNKNOWN`) → 재추가 → 질문(`ANSWERED`)으로 전/후를 다시 확인했다
- 판정: PASS

**3) 도전2 선택과제 — 저장 위치가 테이블로 바뀌었는가**

- 수행: `seed.py`로 FAQ 101건·동의어 10개를 Supabase로 옮긴 뒤, `faq.json`과 `synonyms.json`을 숨기고 실행
- 기대: 파일이 없어도 101건이 정상 응답
- 실제: 첫 시도는 파일이 없다며 죽었다(파일 모드로 떨어져 있었음). `load_env()` 순서를 고친 뒤 재실행하니 `저장 모드: Supabase 테이블 / FAQ 건수: 101`로 정상 응답
- 조치: `app.py`에서 `load_env()`를 import보다 위로 이동
- 재실행: 파일을 숨긴 상태에서 두 질문 모두 정상 응답
- 판정: PASS

**4) 도전3 — 같은 질문 4개를 S4와 S5에 던지기**

- 수행: 데이터 4,705건 고정. 검색만 키워드(S4) → TF-IDF(S5)로 바꿔 같은 질문 4개를 실행

| 질문 | S4 키워드 | S5 TF-IDF |
|---|---|---|
| 굴착기 접수비 얼마예요? | `[ANSWERED]` 14,500원 | `[ANSWERED]` 14,500원 (유사도 0.66) |
| 포크레인 접수비 얼마예요? | `[UNKNOWN]` (손해평가사 문서를 1등으로 가져옴) | `[ANSWERED]` 굴착기 (0.40) |
| 한조기 시험비 얼마예요? | `[UNKNOWN]` (검색 0건) | `[ANSWERED]` 한식조리 (0.44) |
| 요보사 합격 기준이 몇 점이에요? | `[UNKNOWN]` (굴착기 문서) | `[UNKNOWN]` (0.57) |

- 기대: 교재 설명대로 S4에서 틀리던 것이 S5에서 맞는다
- 실제: 4개 중 2개가 고쳐졌고 1개는 그대로였다. 추가로 16문항을 직접 만들어 재보니 S4 10/16(62%) → S5 11/16(68%)이었고, **S5가 새로 틀린 것도 있었다**("한식조리기능사 시험비" → 굴착기)
- 조치: 없음(관찰이 목적)
- 판정: PASS (다만 교재 설명보다 결과가 복잡했다)

**5) 도전4 — 동의어까지 갖추면 "요보사"가 풀리는가**

- 수행: S6에 동의어 기능을 추가한 뒤, 빈 사전 상태에서 "요보사 합격 기준이 몇 점이에요?" 질문 → 동의어 탭에서 `요보사 → 요양보호사` 추가 → 같은 질문 재실행
- 기대: 추가 전 `[UNKNOWN]`, 추가 후 정상 응답
- 실제: 추가 전 `[UNKNOWN] (유사도 0.57)`, 추가 후 `[ANSWERED] 요양보호사 합격 기준은 필기와 실기 각 60% 이상입니다 (유사도 0.64)`
- 조치: 배포본 S6에는 동의어 기능이 없어(탭 2개) S3의 코드를 옮겼다. 강사님 자료의 도전4 구성은 "도전2에서: 관리자 탭(FAQ CRUD + 동의어)"이므로 3탭이 맞다
- 판정: PASS

**6) 서비스 이식 — 검색을 IDF 가중치로 바꾸면 나아지는가**

- 수행: 사이트 문서 21건에 대해 질문 24개를 만들고, 기존 방식(겹치는 낱말 개수)과 IDF 가중치 방식의 검색 결과를 비교
- 기대: 문서가 21건뿐이라 효과가 작을 것으로 예상했다
- 실제: **예상이 틀렸다.**

| 방식 | 1등 정확 | 상위 3개 안에 정답 |
|---|---|---|
| 기존 (낱말 개수) | 13/24 (54%) | 20/24 (83%) |
| IDF 가중치 | 15/24 (63%) | **22/24 (92%)** |

  나빠진 문항은 0건이었다. 사이트는 상위 3개를 Gemini에 넘기므로 실질 지표는 뒤쪽이다.
- 조치: `chat.js`와 `app.py` 양쪽에 IDF를 넣고 `MIN_SCORE`를 2 → 2.5로 다시 잡았다. 점수 단위가 달라졌기 때문이다(관련 없는 질문 7개는 전부 0.00점, 답이 있어야 하는 질문 24개 중 가장 낮은 것이 2.99점이라 그 사이로 정했다)
- 재실행: 두 구현의 점수가 소수점까지 일치하는 것을 확인했다(8.11 / 8.98 / 10.26 / 11.72 / 0). 실기 질문 거절·소관 밖 질문 거절·발주처가 확인 못 한 8가지 항목 거절·근거 없을 때 거절이 모두 정상 동작하는 것도 확인했다
- 판정: PASS

---

### 오전에 알게 된 것 정리

**1. 검색 알고리즘으로 풀리는 문제와, 사람이 넣어줘야 풀리는 문제는 다르다.**

"요보사" 한 질문을 세 단계에 걸쳐 쫓았다.

| 단계 | 결과 | 왜 |
|---|---|---|
| S4 키워드 | 굴착기 문서를 가져옴 | 자격증명이 안 맞아 가장 큰 점수(+3)를 못 받음 |
| S5 TF-IDF | 여전히 못 찾음(0.57) | 데이터에 "요보사"가 한 번도 안 나옴 |
| S6 +동의어 | 해결(0.64) | 사람이 직접 말을 등록 |

같은 줄임말이라도 "포크레인"은 상담 기록에 실제로 나오는 말이라 TF-IDF가 스스로 해결했지만, "요보사"는 데이터에 없어서 사전 등록이 유일한 해법이었다. **데이터에 없는 말은 어떤 검색 방식으로도 못 찾는다.**

**2. TF-IDF는 만능이 아니고, 한국어에는 구멍이 있다.**

`TfidfVectorizer`는 띄어쓰기로만 낱말을 쪼갠다. "한식조리기능사 시험비가 얼마예요?"는 `['한식조리기능사', '시험비가', '얼마예요']`가 되는데, 문서에는 "한식조리"로 적혀 있어 `한식조리기능사`는 사전에 없는 낱말로 취급돼 통째로 버려진다. 남은 "시험비가/얼마예요"만으로 매칭되니 아무 자격증이나 걸린다. 반면 키워드 매칭은 글자가 들어있는지로 보기 때문에 이 경우는 맞췄다. TF-IDF는 영어를 기준으로 만들어진 방식이다.

**3. 예상은 세우되, 재보고 나서 정한다.**

사이트 문서가 21건뿐이라 IDF 효과가 없을 것으로 예상했는데 틀렸다. 문서 **개수**보다 **흔한 낱말이 얼마나 쏠려 있는가**가 중요했다. 이 문서들은 전부 접수 안내라 "접수"·"시험"이 거의 모든 문서에 들어 있어 21건에서도 구분력이 떨어졌다. 재보지 않고 넘어갔으면 개선을 통째로 놓쳤을 것이다.

**4. 오늘 막힌 9건 중 내가 코드를 잘못 짜서 생긴 것은 없었다. 대신 판단을 틀린 것이 있었다.**

| 유형 | 건수 | 내용 |
|---|---|---|
| 바깥이 바뀜 | 3 | 모델 은퇴, 자료 갱신, 허깅페이스 유료화 |
| 받은 코드의 결함 | 3 | SQL 예약어, import 순서, 동의어가 검색에만 적용 |
| 도구 사용 방식 | 1 | GitHub push ≠ 배포 (CLI 배포 프로젝트였음) |
| 내 판단 오류 | 2 | 확인용 질문을 잘못 골라 "배포됐다"고 두 번 오판, 문서가 적으면 IDF 효과가 없을 것이라는 예상 |

실제 개발에서는 내가 쓴 코드보다 **내 밑에 깔린 것들**(외부 API, 남이 만든 자료, 라이브러리 정책)이 더 자주 문제를 만든다. 증상을 보고 원인을 좁히는 순서 — "키 문제인가 모델 문제인가", "저장이 안 된 건가 못 찾는 건가", "검색이 막힌 건가 답변이 막힌 건가" — 를 갖고 있는 것이 중요했다.

**5. 실험실과 서비스는 다른 물건이고, 배운 것을 옮기는 것이 연결이다.**

처음에는 실습 챗봇(Gradio)을 홈페이지에 심을 생각을 했다. 하지만 gradio.live 주소는 내 노트북이 켜져 있어야만 살아 있고, iframe으로 심으면 사이트 디자인과 맞지도 않는다. 며칠 뒤 깨질 링크를 홈페이지에 다는 셈이다. 대신 **실습에서 알아낸 것을 서비스 코드에 옮기는 쪽**을 택했다. 결과적으로 사이트 챗봇이 알아듣는 줄임말이 7개 → 14개가 되었고, 근거를 제대로 찾는 비율이 83% → 92%가 되었다. 링크를 붙였다면 아무것도 좋아지지 않았을 것이다.

---

## 8/20 (목) 오후 — 중간 점검 (13:00 ~ 15:00)

**오후 목표**

담당자가 웹에서 줄임말을 관리할 수 있게 만들고, QA2 체크리스트(도전1~4)를 항목마다 직접 돌려 기록한다.

**한 일** (한 줄씩)

- 실서비스에 「말 바꾸기」 화면 신설 — 담당자가 코드 수정·배포 없이 줄임말을 넣고 끌 수 있게 함
- QA1(어제) 11건 재점검 — 전부 코드에 반영돼 있음을 확인
- QA2 도전1 (13:28~13:45) · 도전2 (13:46~14:18) · 도전3 (14:36~14:54) 수행
- 도전2 M7 FAIL → 원인 추적 후 네 군데 수정하고 재실행해 PASS로 뒤집음

### 막힌 것과 푼 방법 (오후)

#### 10. 「말 바꾸기」 화면이 스타일을 하나도 못 받음

| | |
|---|---|
| 무엇을 하려 했나 | 담당자가 웹에서 줄임말을 관리하도록 `/admin/synonyms.html` 을 만들었다 |
| 화면에 나온 말 | 오류 없음. 다만 버튼이 브라우저 기본 회색으로 나오고 입력칸이 찌그러졌다 |
| 시도한 것 | `admin.css` 를 열어 보니 버튼 스타일이 `.bar button`, `.doc-foot button`, `#lock button` 세 곳에만 걸려 있었다. 내가 쓴 `.card`·`.row` 는 **존재하지 않는 클래스**였다. 기존 「문서 고치기」 화면과 같은 구조(`.doc` / `.doc-head` / `.bar` / `.doc-foot`)로 다시 짰다 |
| 결과 | 기존 어드민과 같은 모양이 됐다. 줄마다 「어르신이 쓰시는 말」·「문서에 적힌 이름」 이름표를 붙이고, 끈 항목은 `.doc.off` 와 배지로 표시했다 |

**알게 된 것**: 남이 만든 CSS 위에 화면을 얹을 때는 **그 CSS가 어떤 이름에 반응하는지** 먼저 봐야 한다. 클래스 이름을 새로 지으면 아무 스타일도 안 붙는다.

#### 11. 삭제가 안 되는데 "삭제 완료"라고 말함

| | |
|---|---|
| 무엇을 하려 했나 | QA2 도전2 M7 — 동의어 "전기사"를 삭제한 뒤 챗봇이 못 찾는지 확인하려 했다 |
| 화면에 나온 말 | `삭제 완료: 전기사` — 그런데 목록에 그대로 남아 있고 챗봇도 계속 답했다 |
| 시도한 것 | Supabase 표를 직접 조회하니 행이 그대로 있었다. 익명 키로 DELETE 를 직접 보내 보니 **HTTP 200 인데 지워진 행은 `[]`** 였다. 원인은 내가 만든 것이었다 — 오후에 실서비스용 「말 바꾸기」를 만들며 `synonyms` 표의 접근 정책을 다시 짤 때 **delete 정책을 일부러 만들지 않았다**(`faq_docs` 와 같은 원칙). 그것이 실습 챗봇의 삭제를 막았다. Supabase 는 권한이 없을 때 에러가 아니라 "0건 처리"로 답하고, `store.py` 는 HTTP 200 만 보고 성공으로 판단한다 |
| 결과 | 네 군데를 고쳤다. ① 실습용 표(`synonyms_practice`) 분리 ② `store.py` 에 "0건이면 실패" 검사 ③ `rag.py` 에서 없는 항목도 잡기 ④ `app.py` 가 오류를 화면 메시지로 표시. 재실행해 M7 을 PASS 로 뒤집었다 |

**알게 된 것**: **한쪽을 안전하게 만든 조치가 다른 쪽을 조용히 망가뜨릴 수 있다.** 그리고 ②만으로는 부족했다. `rag.py` 가 `if short in SYNONYMS:` 로 감싸고 있어 사전에 없으면 표에 요청조차 보내지 않았다. **고친 코드가 실행될 기회가 없었던 것**이다. 한 군데만 고치고 "됐겠지" 하면 안 되고 데이터가 흐르는 길을 끝까지 따라가야 한다.

#### 12. 실습장과 운영이 한쪽만 분리돼 있었음

| | |
|---|---|
| 무엇을 하려 했나 | QA2 도전2 M5 — 실습 챗봇에서 동의어를 추가했다 |
| 화면에 나온 말 | 오류 없음. 다만 목록에 `중장비면허 → 굴착기운전기능사` 가 보였다. 실습에서 넣은 적이 없는 항목이었다 |
| 시도한 것 | 그것은 오후에 **실제 사이트**의 「말 바꾸기」를 시험하며 넣은 것이었다. 표 이름을 확인하니 FAQ 는 `faq`(실습) / `faq_docs`(운영)로 나뉘어 있는데 **동의어만 양쪽 다 `synonyms`** 였다. 배포된 사이트에 "전기사 계산기 가져가도 되나요?"를 물으니 답이 나왔다 — 실습에서 넣은 것이 운영에 그대로 반영된 것이다 |
| 결과 | `synonyms_practice` 표를 새로 만들어 실습을 떼어냈다. 운영 표에서 실습 흔적 3건(전기사·중장비면허·찍차)을 제거했다. 운영 9개 / 실습 11개로 갈렸다 |

**알게 된 것**: **나뉜 것 하나를 보고 전부 나뉘었다고 넘겨짚었다.** 오전에 "FAQ 표 이름이 다르니 충돌하지 않는다"고 판단한 것이 FAQ 에만 해당하는 이야기였는데, 오후에 동의어 기능을 새로 붙이면서 그 연결을 놓쳤다.

#### 13. 확인용 질문을 세 번 잘못 골랐음

| | |
|---|---|
| 무엇을 하려 했나 | 배포·삭제·표 정리가 실제로 반영됐는지 확인하려 했다 |
| 화면에 나온 말 | 오류 없음. 매번 "정답처럼 보이는 답"이 나왔다 |
| 시도한 것 | ① `포클레인 접수비` 로 배포를 확인했는데, 기존 동의어 `접수비 → 응시료` 만으로도 "기능사 필기 응시료" 문서가 걸리고 그 문서에 "종목이 달라도 14,500원"이라 적혀 있어 **포클레인을 몰라도 정답이 나왔다.** 그래서 배포가 안 됐는데 됐다고 두 번 오판했다. 결국 `vercel ls` 로 배포 시각을 직접 봐서 밝혔다 ② `전기사 계산기` 는 「시험 당일 지참물」 문서에서 답이 나온다 ③ `전기사 시험비` 는 FAQ 키워드에 `전기` 가 있어 동의어 없이도 걸린다 |
| 결과 | `한조기` 로 바꾸니 한 번에 갈렸다. `한식조리` 와 글자가 하나도 겹치지 않아 동의어가 없으면 절대 못 찾기 때문이다 |

**알게 된 것**: **확인용 질문은 "그 변경이 없으면 절대 정답이 나올 수 없는 것"이어야 한다.** 다른 경로로도 정답이 나오는 질문을 쓰면 "바뀌었는데 안 바뀐 것처럼", "안 바뀌었는데 바뀐 것처럼" 보인다. 도전1 일지에 이 교훈을 적어 놓고도 두 번 더 걸렸다.

#### 14. git push 와 배포는 다른 일이었음

| | |
|---|---|
| 무엇을 하려 했나 | 줄임말·IDF 개선을 커밋·push 하고 사이트에서 확인하려 했다 |
| 화면에 나온 말 | push 는 성공했는데 사이트 응답이 그대로였다 |
| 시도한 것 | 두 번 더 기다렸다가 확인해도 같았다. `vercel ls` 로 보니 **최신 배포가 20시간 전**이었다. `.vercel` 링크 파일도 없었다. 이 프로젝트는 GitHub 연동 자동 배포가 아니라 **CLI 로 직접 올리는 방식**이었다 |
| 결과 | `starter` 폴더에서 `vercel link --project iny-senior-cert-apply` 후 `vercel --prod` 로 배포했다. 그 뒤 개선이 사이트에 반영된 것을 확인했다 |

**알게 된 것**: 저장소에 올라간 것이 곧 사이트에 반영된 것은 아니다. 프로젝트마다 배포 방식이 다르므로 확인해야 한다.

### QA2 결과 (도전1~4)

| 도전 | 시각 | Must | Should | Could |
|---|---|---|---|---|
| 도전1 RAG 복습 | 13:28~13:45 | 6/6 | 3/3 | 보류1·부분1 |
| 도전2 Month1 복습 | 13:46~14:18 | **8/8** (M7 FAIL→수정 후 PASS) | 4/4 | 2/2 |
| 도전3 시나리오 확장 | 14:36~14:54 | 5/6 (M6 FAIL) | 2/3 (S2 FAIL) | 미실시 |
| 도전4 완성형 | 15:00~15:19 | 5/6 (M2 부분) | 2/3 (S3 FAIL) | 2/2 |

#### 15. 데이터 3,502건(74%)이 검색에 반쪽만 쓰이고 있었음

| | |
|---|---|
| 무엇을 하려 했나 | 도전4 S3 — 서로 다른 자격증 5개를 연속으로 물어 안정성을 보려 했다 |
| 화면에 나온 말 | "공인중개사 환불 규정" 이 **유사도 0.77**(오늘 두 번째로 높은 값)인데도 `[UNKNOWN]` |
| 시도한 것 | 검색 결과를 열어 보니 1~3등 문서의 근거가 **모두 비어 있었다.** 데이터가 두 출처를 합친 것인데 한쪽만 읽고 있었다. 게시판 1,203건은 `title/body/reply`, 전화 상담 3,502건은 `opening/exchanges` 인데 `_build_docs()` 가 앞의 칸 이름만 이어 붙였다. 그래서 전화 기록의 검색용 글자가 `'공인중개사 HC 환불   '` 이 됐다. 자격증명과 분류만 남고 내용이 통째로 빠진 것이다. 정작 그 안에는 답이 들어 있었다 — "환불은 접수기간 안에 취소하시면 100%, 5일 전까지는 50%…" |
| 결과 | 네 군데를 고쳤다. ① `_question_of()`·`_answer_of()` 로 두 출처를 모두 읽게 함(근거 빈 문서 3,502 → 0건) ② `CERT_WEIGHT=5`(내용을 넣자 문서가 길어져 자격증명이 묻혔다. 24문항으로 재어 정함: 1번 17/24 → 5번 24/24) ③ 프롬프트에 자격증 확인 문구 ④ 출처 이름 보완. 검증: 답해야 하는 8문항 8/8, 거절해야 하는 5문항 5/5 |

**알게 된 것**: **문서가 짧을수록 유사도가 높게 나온다.** 내용이 없어서 유사도가 높아지고, 내용이 없어서 답을 못 하는 상태였다. 오늘 유사도를 "얼마나 확신하는지"로 읽어 왔는데, 실제로는 **가장 높은 값(0.77)이 가장 쓸모없는 문서였고 가장 낮은 값(0.33)이 정답**이었다. 유사도는 정답 여부와 나란히 가지 않는다.

#### 16. 검색을 고치니 더 위험한 구멍이 드러남

| | |
|---|---|
| 무엇을 하려 했나 | 15번을 고친 뒤 잘 되는지 확인하려 했다 |
| 화면에 나온 말 | `미용사 시험비` → **`[ANSWERED] 미용사 시험의 1차 응시료는 20,000원이며, 2차는 33,000원입니다`** (출처: 손해평가사) |
| 시도한 것 | 우리가 다루지도 않는 자격증에 **손해평가사 요금을 미용사 요금이라고 안내**했다. 전화 상담 기록에는 본문에 자격증명이 안 나온다. 통화하는 두 사람은 이미 알고 있어 말하지 않기 때문이다 — "1차 응시료는 20,000원이에요. 2차는 별도로 33,000원을 내셔야 해요." 이 문장만 넘기니 Gemini 가 미용사 얘기로 받아들였다 |
| 결과 | 프롬프트에 "이 근거는 '손해평가사' 에 대한 것입니다. 질문이 다른 자격증이면 UNKNOWN 이라고 답하세요" 를 넣었다. 미용사·제빵기능사 등 새로 넣어 본 자격증까지 모두 거절한다 |

**알게 된 것**: **고치기 전에는 근거가 비어 있어 "우연히" 막혀 있었다.** 잘 되고 있던 것이 아니라 망가져서 안 되고 있던 것이다. 하나를 고치면 그 뒤에 가려져 있던 문제가 드러난다. 고친 뒤에 **반대 방향(거절해야 하는 것)도 함께 확인해야 한다.**

#### 17. 실습에서 고친 것을 배포 사이트에 옮기며 — 그대로 복사하면 안 됐다

| | |
|---|---|
| 무엇을 하려 했나 | 15·16번에서 고친 것을 배포된 접수 사이트 챗봇에 반영하려 했다 |
| 화면에 나온 말 | 오류 없음. 다만 두 챗봇의 데이터 구조가 달랐다 |
| 시도한 것 | 실습 S6 는 상담 기록 4,705건(`exchanges`)을 읽고, 배포 사이트는 안내 문서 23건(`title/content`)을 읽는다. **`exchanges` 문제 자체가 사이트에는 없다.** 그래서 옮길 것과 안 옮길 것을 갈라야 했다. 배포된 사이트에 직접 물어 확인하니 — `정보처리기사 환불 규정` → **환불 규정을 그대로 안내함**, `미용사 환불 규정` → 거절. 같은 종류인데 결과가 갈렸다. 모델 판단에 맡기면 그때그때 다르다 |
| 결과 | 자격증 오배정 방지만 옮기되 **규칙으로** 만들었다. `outOfScopeCert()` 가 "정보처리기사" 같은 자격증 이름꼴을 찾아 우리 8종·별칭에 없으면 안내문으로 끊는다. 안내 문구는 문서에서 읽어 담당자가 고칠 수 있게 했다. 오탐 0건 / 놓침 0건 |

**알게 된 것**: **오전에 미결로 남긴 문제가 여기서 풀렸다.** `전기기사`는 전기기능사와 다른 상위 등급 자격증인데 코드의 `"전기"` 동의어 때문에 전기기능사 정보로 안내될 수 있었다. 오전에는 "동의어를 빼면 정상 질문도 막힌다"며 판단을 미뤘는데, **동의어를 빼는 대신 자격증 판별을 따로 두는 것**으로 둘 다 지켰다. 지금은 `전기기사`는 끊고 `전기기능사`·`전기 시험비`는 그대로 통과한다.

### 오후에 알게 된 것

**1. 교재의 전제가 실제와 달랐다.**

교재는 "40건에서는 정확, 4,705건에서는 부정확"이라고 하지만 **40건에서도 틀렸다.**
`굴착기 시험비` 에서 정답 문서가 1점 차이로 2등이 되어 Gemini 에게 전달되지 않았다.
`시험` 이 `시험비` 안에 들어 있어 「시험과목」 문서가 같은 점수를 받고 제목 겹침으로 이겼다.
**데이터가 많아져서 나빠지는 것이 아니라, 키워드 매칭이라는 방식 자체가** 흔한 낱말이
겹칠 때 순위를 잘못 매긴다. 4,705건에서는 그 일이 더 자주 일어날 뿐이다.

**2. TF-IDF 는 일관되게 낫지 않다. 1승 1패 1무였다.**

| 질문 | 키워드 | TF-IDF |
|---|---|---|
| 포크레인 접수비 | ❌ | ✅ |
| 요보사 합격 기준 | ✅ | ❌ |
| 굴착기 접수비 | ✅ | ✅ |

두 방식은 원인이 다른데 **증상이 같다.** 키워드는 `시험` 이 `시험비` 안에 들어가서,
TF-IDF 는 `시험비` 가 너무 희귀해서(4,705건 중 130건) 자격증명을 눌러 버린다.
결국 **한 낱말이 순위를 지배해 자격증이 어긋난다**는 점은 똑같다.

**3. "맞았다"를 그대로 믿으면 안 된다 — 우연한 정답을 두 번 만났다.**

`한조기 시험비` 가 한식조리 답을 냈지만, `한조기` 는 TF-IDF 사전에 없어 **버려진** 상태였다.
사실상 `시험비` 만 검색한 것이고 그 1등이 우연히 한식조리였다(자격증명 없이 `시험비` 만
물었을 때와 유사도까지 0.56 으로 동일했다).
`요보사 합격 기준` 도 키워드 쪽에서 맞았지만 **2점 동점이었고 ID 순서로 1등이 정해진 것**이다.
같은 질문을 길게 던지면 굴착기가 1등이 되어 틀린다.

화면만 보면 구분할 수 없다. **점수를 열어 봐야 알 수 있다.** 정답률만 세면 잘못된 결론에 이른다.

**4. 마지막 방어선이 두 겹이라는 것이 확인됐다.**

① 검색 점수가 문턱에 못 미치면 Gemini 를 아예 부르지 않는다
② 근거를 받아도 질문과 맞지 않으면 Gemini 가 거절한다

"미용사 시험비"(다루지 않는 자격증)를 물었을 때 TF-IDF 는 유사도 0.56 으로 문서를 찾아
Gemini 에게 넘겼다. 검색은 헐거웠지만 ②가 막아 잘못된 안내로 이어지지 않았다.
날씨 질문보다 어려운 경우다. 자격증 얘기라 관련 문서가 실제로 걸리기 때문이다.

**5. 하루에 같은 종류의 실수를 세 번 했다.**

전부 "확인용 질문을 잘못 고른 것"이다. 바꾼 것과 무관한 경로로도 정답이 나오는 질문을
써서 판정이 뒤집혔다. 도전1 일지에 교훈을 적고도 두 번 더 걸렸다.
**기록하는 것과 몸에 붙는 것은 다르다.**

**6. "조용히 실패하는" 문제를 하루에 네 번 만났다.**

| # | 무엇 | 겉으로는 |
|---|---|---|
| 1 | `.env` 에 Supabase 를 넣어도 안 켜짐 | 에러 없음 · 화면 동일 |
| 2 | `git push` 했는데 배포 안 됨 | push 성공 메시지 |
| 3 | 동의어 삭제가 막혔는데 "삭제 완료" | 성공 메시지 |
| 4 | 상담 기록 3,502건이 빈 근거로 검색됨 | 유사도 0.77 (오히려 높음) |

넷 다 **실패했는데 성공처럼 보였다.** 에러가 나면 바로 알아채지만 조용히 실패하면
한참 뒤에야 이상을 느낀다. 그래서 오늘 이런 습관이 생겼다.

- **메시지가 아니라 결과로 확인한다.** "삭제 완료"를 믿지 말고 표를 조회한다.
- **확인용 질문은 그 변경이 없으면 정답이 나올 수 없는 것으로 고른다.**
- **한 군데 고치고 "됐겠지" 하지 않는다.** `store.py` 를 고쳤는데 `rag.py` 가 그 앞에서
  막고 있어 고친 코드가 실행조차 안 된 일이 있었다. 데이터가 흐르는 길을 끝까지 따라간다.

**7. 하나를 고치면 뒤에 가려져 있던 문제가 드러난다.**

상담 기록을 읽게 하자 "미용사 시험비"에 손해평가사 요금을 안내하는 일이 생겼다.
**고치기 전에는 근거가 비어 있어 우연히 막혀 있던 것**이다.
잘 되고 있던 것이 아니라 망가져서 안 되고 있던 것이다.
그래서 고친 뒤에는 **반대 방향(거절해야 하는 것)도 함께 확인해야 한다.**
오늘은 "답해야 하는 8문항"과 "거절해야 하는 5문항"을 한 묶음으로 재는 방식으로 굳혔다.

**8. 실험실에서 고친 것을 그대로 서비스에 복사하면 안 된다.**

실습 S6 는 상담 기록 4,705건(`exchanges`)을 읽고 배포 사이트는 안내 문서 23건
(`title/content`)을 읽는다. 네 가지를 고쳤지만 **사이트에 옮긴 것은 한 가지**뿐이다.
나머지 셋은 사이트에 해당하지 않는 문제였다.
무엇을 옮길지는 짐작하지 않고 **배포된 사이트에 직접 물어서** 정했다.

**9. 미룬 판단이 다른 방법으로 풀리기도 한다.**

오전에 `전기기사` 문제를 "동의어를 빼면 정상 질문도 막힌다"며 미결로 남겼다.
오후에 **동의어를 빼는 대신 자격증 판별을 따로 두는 방법**으로 둘 다 지켰다.
지금은 `전기기사` 는 끊고 `전기기능사`·`전기 시험비` 는 그대로 통과한다.
답이 안 보일 때는 **선택지가 둘뿐이라고 생각한 것이 문제**일 수 있다.

---

**완성한 주소 3개**

- 접수 사이트: https://iny-senior-cert-apply.vercel.app
- 챗봇: https://iny-senior-cert-apply.vercel.app/faq.html
- 어드민: https://iny-senior-cert-apply.vercel.app/admin/integrated.html
- 말 바꾸기(신설): https://iny-senior-cert-apply.vercel.app/admin/synonyms.html

GitHub: https://github.com/wkddlsdud971014-young/iny-senior-cert-apply

---

## 테스트 수행 일지

**1) 접수 → DB 저장**

수행: 접수 화면에서 한식조리기능사 카드를 고르고 이름 "김두두", 연락처 "01012345678"을 넣고 「신청서 보내기」를 눌렀다
기대: Supabase `applications` 테이블에 행 1개
실제: 화면에 접수번호 1이 크게 떴고, Supabase Table Editor를 새로고침하니 `1 record`로 김두두 행이 보였다
조치: 없음
재실행: 두 번째로 "장이니"를 접수하니 접수번호 2, 표에도 2줄이 됐다
판정: PASS

**2) 어드민 시각 표시**

수행: 어드민 화면을 열어 접수일시를 확인했다
기대: 접수 완료 화면과 같은 `11:11`
실제: Supabase 원본은 `02:11:12`(UTC). 그대로 두면 9시간 어긋난다
조치: 어드민에서 `Asia/Seoul` 기준으로 변환해 표시하도록 고쳤다
재실행: 어드민 표에 `2026-08-19 11:11` 로 나왔다
판정: PASS

**3) 배포 후 문서 노출 여부**

수행: 배포 주소에 `/client-docs/` 를 붙여 접속했다
기대: 404 (클라이언트 문서는 공개되면 안 됨)
실제: 404
조치: 없음. 배포 전에 Root Directory를 `starter`로 지정해 둔 것이 맞게 동작했다
재실행: `/` 200, `/admin/` 200, `/client-docs/` 404 를 한 번 더 확인했다
판정: PASS



예시

 수행: 폼에 "홍길동 / 한식조리기능사" 넣고 제출
  기대: Supabase applications 테이블에 행 1개
  실제: 화면이 뜨지 않음
  조치: API 키 변수명이 소문자였음 -> 대문자로 변경
  재실행: 행 1개 생성 확인
  판정: PASS
