# =============================================================
# 챗봇 설정값 — 여기 숫자만 바꾸면 챗봇 성격이 달라집니다.
# =============================================================

# 근거로 쓸 문서를 몇 개까지 찾을지
TOP_K = 3

# 점수가 이 값보다 낮으면 "모르겠습니다"라고 답합니다.
#   낮추면 → 근거가 약해도 답합니다 (지어낼 위험 ↑)
#   높이면 → 근거가 있어도 거절합니다 (모른다는 답 ↑)
# 2026-08-20: 낱말 개수 세기에서 IDF 가중치로 바꾸면서 점수 단위가 달라져 다시 쟀습니다.
#   관련 없는 질문(날씨·주식·점심) 7개 → 전부 0.00점
#   답이 있어야 하는 질문 24개        → 가장 낮은 것이 2.99점
# 그 사이인 2.5 로 잡았습니다. api/chat.js 의 MIN_SCORE 와 같아야 합니다.
MIN_SCORE = 2.5

# Gemini 로 문장을 다듬을지
#   False 로 두면 문서 내용을 그대로 보여 줍니다 (지어낼 위험 0)
USE_GEMINI = True

# 쓸 모델. 하나가 안 되면 다음 것을 시도합니다.
# 쓸 모델. 이 하나로 고정합니다.
GEMINI_MODELS = [
    "gemini-3.5-flash-lite",
]
