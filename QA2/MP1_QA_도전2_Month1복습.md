# 도전2 QA - Month 1 복습 (S3)

8/20(목) - 테스트 범위: 관리자 페이지 (FAQ CRUD + 동의어 관리)

> 작성자: 장인영 (iny)
> 테스트 시작: **13:46**   테스트 종료: **14:18**   (32분)
> 결과: Must 7/8 (M7 FAIL) · Should 4/4 · Could 2/2
> 대상: S3 `https://32b927a8aeeef7c6fc.gradio.live` (로컬 `http://localhost:7865`)
>
> **저장 위치 참고**: 이 단계에서 FAQ·동의어의 저장 위치를 파일(`faq.json`·`synonyms.json`)에서
> **Supabase 표**로 옮겨 두었다(도전2 선택 과제). 그래서 S3·S4 항목의 "파일에 저장되는가"는
> "표에 저장되는가"로 바꿔 확인한다.

전제: 도전1 Must 전부 PASS

---

## Must - 반드시 통과

### M1. 3탭이 모두 보인다

| 단계 | 내용 |
|---|---|
| 수행 | stage3_share_admin에서 실행 후 공유 URL(`https://32b927a8aeeef7c6fc.gradio.live`) 접속 |
| 기대 | 상단에 "챗봇", "FAQ 관리", "동의어 관리" 3개 탭 |
| 실제 | 3개 탭이 모두 보인다. 챗봇 탭 제목은 "자격증 시험 접수 FAQ 챗봇", 설명은 **"8개 자격증 FAQ 100건 + 동의어 사전으로 줄임말도 인식합니다"**. 예시 질문 4개(한식조리기능사 시험비 / 요보사 합격 기준 / 포크레인 접수는 어디서 / 전기기사 계산기) |
| 조치 | 처음 접속했을 때 예전 프로세스(Supabase 전환 전 버전)가 포트 7865를 잡고 있어 그것을 끝내고 새로 띄웠다 |
| 재실행 | 포트 정리 후 재실행 → 3탭 정상 |
| 확인 | 3탭 표시 **O** |
| 판정 | **PASS** |

> 도전1의 S1·S2 는 `gr.ChatInterface` 한 줄로 만든 채팅 화면 하나뿐이었다.
> S3 는 `gr.Blocks` 로 화면 구조를 직접 짜서 탭을 넣었다.
> **관리자가 코드를 건드리지 않고 FAQ를 고칠 수 있게 된 것**이 이 단계의 핵심이다.
>
> 데이터도 40건에서 **100건**으로 늘어 있다(화면 설명에 표시됨).
> 도전1의 S1 에서 답하지 못한 "손해평가사 1차 2차"가 여기서는 답이 되는지 뒤에서 볼 수 있다.

### M2. FAQ 관리 탭에서 새 FAQ를 추가한다

| 단계 | 내용 |
|---|---|
| 수행 | FAQ 관리 탭 > 자격증 "한식조리기능사", 제목 "테스트 FAQ", 답변 내용 "테스트 답변입니다", 키워드 "테스트" > FAQ 추가 버튼 |
| 기대 | "추가 완료" 메시지 + 목록에 새 항목 |
| 실제 | 결과 칸에 **`추가 완료: 테스트 FAQ`**. Supabase `faq` 표를 직접 조회하니 **`CUSTOM_102 / 한식조리기능사 / 테스트 FAQ`** 로 들어가 있고 전체 건수가 101 → **102건**이 되었다 |
| 조치 | - |
| 재실행 | - |
| 확인 | 목록에 "테스트 FAQ" **O** (ID: `CUSTOM_102`) |
| 판정 | **PASS** |

> 화면 설명에는 아직 "FAQ를 추가/삭제하면 **faq.json**이 바뀌고"라고 적혀 있으나,
> 실제 저장 위치는 Supabase `faq` 표다. 저장 위치를 옮기면서(도전2 선택 과제)
> 안내 문구를 같이 고치지 않은 것이다. 동작에는 문제가 없고 설명만 옛것이다.
> → **고칠 것 목록에 넣는다.**
>
> `CUSTOM_101` 은 오전에 MUST 챌린지로 넣은 "한식조리기능사 실기 복장 규정"이다.
> 새로 넣은 것이 102번으로 이어 붙은 것을 보면 번호가 겹치지 않게 매겨지고 있다.

### M3. 추가한 FAQ로 챗봇이 답변한다

| 단계 | 내용 |
|---|---|
| 수행 | 챗봇 탭 > "테스트" 입력 |
| 기대 | 방금 추가한 "테스트 답변입니다"가 답변에 포함 |
| 실제 | `[ANSWERED] 테스트 답변입니다.`<br>`출처: 한식조리기능사 - 테스트 FAQ` |
| 조치 | - |
| 재실행 | - |
| 확인 | 추가한 내용이 답변에 나오는가 **O** |
| 판정 | **PASS** |

> 서버를 다시 켜지 않고 관리 탭에서 넣은 것이 곧바로 검색에 잡혔다.
> `add_faq_entry()` 뒤에 `reload_faq()` 를 부르기 때문이다. 이것을 빠뜨리면
> 저장은 되지만 챗봇은 예전 데이터로 답한다.
>
> 오전 MUST 챌린지에서는 추가했는데도 `[UNKNOWN]` 이 나온 적이 있었다.
> 그때는 저장·갱신 모두 정상이었고 **기존 FAQ 가 점수 싸움에서 이겨**
> 정답 문서가 Gemini 에게 전달되지 않은 것이 원인이었다(8점 대 5점).
> 이번에는 "테스트"라는 낱말이 102건 중 이 문서에만 있어 문제없이 1등이 됐다.

### M4. FAQ를 삭제하면 목록에서 사라진다

| 단계 | 내용 |
|---|---|
| 수행 | FAQ 관리 탭 > 「삭제할 FAQ ID」에 `CUSTOM_102` 입력 > 삭제 버튼 |
| 기대 | "삭제 완료" + 목록에서 사라짐 |
| 실제 | 결과 칸에 **`삭제 완료: CUSTOM_102`**. Supabase `faq` 표를 조회하니 102 → **101건**, `CUSTOM_102` 가 표에서도 사라졌다. 남은 CUSTOM 항목은 `CUSTOM_101` 하나뿐 |
| 조치 | - |
| 재실행 | - |
| 확인 | 사라졌는가 **O** (화면과 표 양쪽에서 확인) |
| 판정 | **PASS** |

> 이것으로 관리자 기능 한 바퀴(추가 → 검색 반영 → 삭제)가 모두 돌았다.
>
> 다만 이 삭제는 **되돌릴 수 없다.** 실습용이라 그대로 두었지만,
> 실제 서비스(`/admin/faq.html`, `/admin/synonyms.html`)에는 삭제 버튼을 넣지 않고
> `is_active` 를 내리는 「끄기」만 두었다. 지운 문서는 복구할 수 없고,
> 담당자가 실수로 누를 수 있기 때문이다.
> 같은 기능이라도 실습용과 납품용의 판단이 달라지는 지점이다.

### M5. 동의어를 추가한다

| 단계 | 내용 |
|---|---|
| 수행 | 동의어 관리 탭 > 줄임말 "전기사", 정식 명칭 "전기기능사" > 추가 |
| 기대 | "추가 완료" + 목록에 "전기사 -> 전기기능사" |
| 실제 | 결과 칸에 **`추가 완료: 전기사 -> 전기기능사`**. 목록에도 나타났다.<br>**그런데 목록에 `중장비면허 → 굴착기운전기능사` 가 함께 보였다.** 이것은 실습에서 넣은 것이 아니라 오후에 **실제 사이트**의 「말 바꾸기」 기능을 시험하며 넣은 항목이다 |
| 조치 | 표 이름을 확인했다. 실습 `store.py` 는 `faq` · **`synonyms`**, 실서비스 `chat.js` 는 `faq_docs` · **`synonyms`** 를 쓴다. **FAQ 는 표가 나뉘어 있는데 동의어는 같은 표를 공유하고 있었다.** 배포된 사이트에 "전기사 계산기 가져가도 되나요?"를 물으니 정상 답변이 나왔다 — 실습에서 넣은 것이 운영에 그대로 반영된 것이다. M7 에서 이 항목을 삭제하므로 QA 는 그대로 진행하고, 표 분리는 QA 종료 후 별도로 처리하기로 했다 |
| 재실행 | - |
| 확인 | 목록에 보이는가 **O** |
| 판정 | **PASS** (기능은 정상 · 별도 문제 발견) |

> **실습장과 운영이 한쪽만 분리돼 있었다.**
> 오전에 Supabase 를 붙일 때 "FAQ 표 이름이 달라 충돌하지 않는다"고 판단했는데,
> 오후에 실서비스에 「말 바꾸기」를 만들면서 `synonyms` 표를 읽게 한 것이
> 실습이 쓰던 표와 같은 이름이라는 것을 그때 연결하지 못했다.
> 나뉜 것 하나를 보고 전부 나뉘었다고 넘겨짚은 것이다.
>
> 또 하나. `전기사` 는 오전에 실서비스 동의어를 정리할 때 **일부러 뺐던 종류**다.
> 전기기사는 전기기능사보다 상위 등급의 다른 자격증이라, 접수 안내에서 이렇게
> 이어 주면 전기기사를 접수하려는 분이 엉뚱한 정보를 받게 된다.
> QA 문서가 지시한 항목이라 실습에서는 그대로 넣되, 이 판단은 기록해 둔다.
> → **고칠 것 목록에 넣는다.**

### M6. 줄임말로 질문하면 답이 나온다

| 단계 | 내용 |
|---|---|
| 수행 | 챗봇 탭 > "전기사 시험비" 입력 |
| 기대 | "전기기능사"로 검색되어 관련 답변 |
| 실제 | `[ANSWERED] 전기기능사 필기시험 응시료는 14,500원이고, 실기시험 응시료는 106,000원입니다. 결제는 큐넷에서 온라인으로 합니다.`<br>`출처: 전기기능사 - 전기기능사 응시료` |
| 조치 | - |
| 재실행 | - |
| 확인 | 답변에 "전기기능사" 포함 **O** |
| 판정 | **PASS** |

> 질문에는 "전기사"라고만 썼는데 "전기기능사" 문서를 찾아왔다.
> 동의어는 **검색 직전에 질문의 낱말을 바꿔치기하는 방식**이라
> 문서에 "전기사"라는 말이 한 글자도 없어도 찾을 수 있다.
>
> 순서: `"전기사 시험비"` → 사전이 `"전기기능사 시험비"` 로 바꿈 → FAQ 검색 → 답변

### M7. 동의어 없이 같은 줄임말로 질문하면 못 찾는다

| 단계 | 내용 |
|---|---|
| 수행 | 동의어 관리 탭 > 「삭제할 줄임말」에 "전기사" 입력 > 삭제 > 챗봇 탭에서 "전기사 시험비" 입력 |
| 기대 | [NOT_FOUND] 또는 엉뚱한 답변 |
| 실제 | **삭제가 되지 않았다.**<br>· 결과 칸: `삭제 완료: 전기사` (성공했다고 표시)<br>· 그런데 동의어 목록에 `전기사 → 전기기능사` 가 **그대로 남아 있음**<br>· 챗봇도 M6 과 똑같이 `[ANSWERED] 전기기능사 필기시험 응시료는 14,500원…` 응답<br>· Supabase `synonyms` 표를 직접 조회 → `전기사` 행 그대로 존재<br>· 익명 키로 DELETE 를 직접 보내 보니 **`HTTP 200` 인데 지워진 행은 `[]`** |
| 조치 | 원인을 찾았다. 오후에 실서비스용 「말 바꾸기」 화면을 만들면서 `synonyms` 표의 접근 정책을 다시 짰는데, 그때 **delete 정책을 일부러 만들지 않았다.**<br>`drop policy "syn open"` 으로 "아무나 다 가능"을 걷어내고 select·update·insert 세 개만 열었다. 실수로 지우는 일을 막기 위한 것이고 `faq_docs` 와 같은 원칙이다.<br>그 결과 실습 챗봇의 삭제가 막혔다. Supabase 는 권한이 없을 때 **에러를 내지 않고 "0건 처리"로 응답**하고, `store.py` 는 HTTP 200 만 보고 성공으로 판단해 "삭제 완료"를 출력한다.<br>QA 를 먼저 끝내기로 하고, 조치는 뒤로 미뤘다 |
| 재실행 | 미실시 (QA 종료 후 수정 예정) |
| 확인 | 동의어 유무 차이 기록: **차이를 확인하지 못함.** 삭제 자체가 되지 않아 "동의어 없는 상태"를 만들 수 없었다 |
| 판정 | **FAIL** |

### 고칠 것 (QA 종료 후)

| # | 무엇 | 왜 |
|---|---|---|
| 1 | 실습용 표를 운영과 분리 (`synonyms` 공유 중) | 실습에서 넣고 지운 것이 배포된 사이트에 그대로 반영된다. M5 에서 확인 |
| 2 | `store.py` 가 "0건 처리"를 성공으로 보지 않게 | 지워지지 않았는데 "삭제 완료"라고 말한다. 담당자가 지운 줄 알고 넘어가게 된다 |

> **오늘 세 번째로 만나는 "조용히 실패하는" 문제다.**
>
> | | 언제 | 증상 |
> |---|---|---|
> | 1 | 오전 | `.env` 에 Supabase 를 넣어도 안 켜짐. 에러 없음 |
> | 2 | 오후 | `git push` 했는데 배포 안 됨. 에러 없음 |
> | 3 | 지금 | "삭제 완료"인데 안 지워짐. 에러 없음 |
>
> 세 건 모두 **실패했는데 성공처럼 보이는** 형태다. 에러가 나면 바로 알아채지만,
> 조용히 실패하면 한참 뒤에야 이상을 느낀다.
> 이번 건은 QA 항목이 있었기 때문에 발견됐다. 없었다면 "삭제 완료"를 믿고 넘어갔을 것이다.

### M8. 공유 URL에서도 관리 탭이 작동한다

| 단계 | 내용 |
|---|---|
| 수행 | 공유 URL(`https://32b927a8aeeef7c6fc.gradio.live`)에서 FAQ 관리 탭 > 지게차운전기능사 / "공유URL 테스트" / "공유 링크에서 추가한 항목입니다" / 키워드 "공유테스트" 추가 |
| 기대 | 추가/삭제가 된다 |
| 실제 | 결과 칸에 **`추가 완료: 공유URL 테스트`**. Supabase `faq` 표에 **`CUSTOM_102 / 지게차운전기능사 / 공유URL 테스트`** 로 들어갔고 101 → 102건이 되었다. M1~M8 전 과정을 공유 URL 에서 진행했다 |
| 조치 | - |
| 재실행 | - |
| 확인 | 공유 URL에서 관리 **O** |
| 판정 | **PASS** |

> **M7 의 진단이 여기서 확증됐다.** FAQ 는 추가도 삭제도 되는데(M2·M4·M8) 동의어만
> 삭제가 안 된다(M7). 오전에 만든 `faq` 표는 `for all` 정책 그대로고,
> 오후에 접근 정책을 다시 짠 것은 `synonyms` 표 하나뿐이기 때문이다.
> 같은 코드·같은 화면인데 표마다 권한이 달라 한쪽만 막힌 것이다.
>
> **보안 면에서 짚을 것.** 이 공유 URL 은 비밀번호가 없다.
> 링크를 아는 사람은 누구나 FAQ 를 넣고 지울 수 있다. 실습용이라 그대로 두지만,
> 우리가 만든 실서비스 어드민(`/admin/`)에는 비밀번호가 걸려 있다.
> 관리 기능을 바깥에 여는 것과 아무나 쓰게 두는 것은 다른 문제다.

---

## Should - 되어야 하지만 FAIL이면 내일 수정

### S1. 빈 칸으로 FAQ 추가 시 안내 메시지

| 단계 | 내용 |
|---|---|
| 수행 | 제목·답변 내용·키워드를 모두 비우고 FAQ 추가 버튼 |
| 기대 | "제목과 내용을 입력하세요" |
| 실제 | 결과 칸에 **`제목과 내용을 입력하세요`**. Supabase `faq` 표는 102건 그대로이고 **제목이 빈 행도 없다** — 저장되지 않고 막혔다 |
| 조치 | - |
| 재실행 | - |
| 확인 | 안내 메시지 **O** |
| 판정 | **PASS** |

> 빈 값이 그대로 저장되면 검색에 걸려 챗봇이 빈 답을 내놓을 수 있다. 막는 것이 맞다.
>
> 확인하면서 헷갈린 점 하나. 화면 아래 「결과」 칸에 `삭제 완료: CUSTOM_102` 가
> 남아 있었는데 이는 M4 때 나온 메시지가 지워지지 않은 것이다.
> Gradio 는 결과 칸을 다음 동작 때까지 그대로 둔다.
> **화면에 남아 있는 메시지가 방금 일어난 일이라는 보장은 없다.**
> 표를 직접 확인해서 실제 상태와 맞춰 봐야 했다.

### S2. 빈 칸으로 동의어 추가 시 안내 메시지

| 단계 | 내용 |
|---|---|
| 수행 | 줄임말·정식 명칭을 모두 비우고 동의어 추가 버튼 |
| 기대 | "줄임말과 정식 명칭 모두 입력하세요" |
| 실제 | 결과 칸에 **`줄임말과 정식 명칭 모두 입력하세요`**. 목록에 빈 행이 생기지 않았다 |
| 조치 | - |
| 재실행 | - |
| 확인 | 안내 메시지 **O** |
| 판정 | **PASS** |

> 입력 검사는 FAQ 쪽(S1)과 동의어 쪽 모두 걸려 있다.
> M7 에서 삭제는 한쪽만 막혀 있었던 것과 대비된다.
> 검사는 **코드**가 하고 삭제 가능 여부는 **표의 권한**이 정하기 때문이다.
> 코드는 두 곳에 똑같이 짜여 있었지만, 표 권한은 한쪽만 바뀌어 있었다.

### S3. 재시작 후 추가한 FAQ가 남아있다

| 단계 | 내용 |
|---|---|
| 수행 | Ctrl+C 로 서버 중지 → **`faq.json` 을 폴더에서 아예 치운 뒤** 다시 실행 → FAQ 관리 탭 |
| 기대 | 추가한 FAQ가 그대로 (원문 기준: faq.json에 저장) |
| 실제 | 파일이 없는데도 정상 기동했고 목록이 그대로 남아 있었다.<br>`CUSTOM_101 / 한식조리기능사 / 한식조리기능사 실기 복장 규정`<br>`CUSTOM_102 / 지게차운전기능사 / 공유URL 테스트` (M8 에서 넣은 것)<br>그 밖에 `GONGIN_*`, `GULCHAK_*` 등 기본 항목도 모두 표시 |
| 조치 | 시험 후 `faq.json` 을 원위치로 되돌렸다 |
| 재실행 | 새 공유 URL `https://37b714ebe56b6ab249.gradio.live` 로 기동(Gradio 는 켤 때마다 새 주소를 만든다) |
| 확인 | 재시작 후 유지 **O** — 단, **파일이 아니라 Supabase `faq` 표에서 온 것** |
| 판정 | **PASS** |

> 원문은 "faq.json 에 저장되는가"를 묻지만, 도전2 선택 과제로 저장 위치를 표로 옮겼다.
> 파일이 있든 없든 재시작하면 남아 있으므로 그것만으로는 **어디에 저장됐는지** 구분할 수 없다.
> 그래서 **파일을 아예 치운 채로** 켜서 확인했다. 파일이 없는데 데이터가 있으면
> 표에서 왔다는 것 말고는 설명이 되지 않는다.

### S4. 재시작 후 추가한 동의어가 남아있다

| 단계 | 내용 |
|---|---|
| 수행 | S3 와 함께 진행. **`synonyms.json` 도 폴더에서 치운 뒤** 재실행 → 동의어 관리 탭 + 챗봇에서 "전기사 시험비" |
| 기대 | 추가한 동의어가 그대로 (원문 기준: synonyms.json에 저장) |
| 실제 | 목록에 8개가 그대로 남아 있었다.<br>`개사·공개사 → 공인중개사` / `굴삭기 → 굴착기` / `손평사 → 손해평가사` / `요보사 → 요양보호사` / **`전기사 → 전기기능사`**(M5) / `중장비면허 → 굴착기운전기능사` / `지게차면허 → 지게차운전기능사`<br>챗봇도 `[ANSWERED] 전기기능사 필기시험 응시료는 14,500원이고 실기시험은 106,000원입니다` 로 정상 응답 |
| 조치 | 시험 후 `synonyms.json` 을 원위치로 되돌렸다 |
| 재실행 | - |
| 확인 | 재시작 후 유지 **O** — 파일이 아니라 Supabase `synonyms` 표에서 온 것 |
| 판정 | **PASS** |

> 목록에 `중장비면허` 가 있는 것이 M5 에서 발견한 표 공유 문제의 흔적이다.
> 이것은 실습에서 넣은 것이 아니라 **실제 사이트**의 「말 바꾸기」 기능을 시험하며 넣은 항목인데,
> 두 곳이 같은 `synonyms` 표를 쓰기 때문에 여기에도 보인다.

---

## Could - 있으면 좋지만 필수 아님

### C1. FAQ 목록 새로고침 버튼 작동

| 단계 | 내용 |
|---|---|
| 수행 | FAQ 관리 탭 · 동의어 관리 탭에서 「목록 새로고침」 버튼 |
| 기대 | 현재 내용이 다시 표시 |
| 실제 | 목록은 정상적으로 다시 표시된다. 다만 **누르면 화면이 「챗봇」 탭으로 넘어간다.** 다시 해당 탭을 눌러 들어가면 목록이 제대로 갱신돼 있다 |
| 조치 | 고치지 않았다. 기능은 정상이고 보고 있던 탭이 바뀌는 불편만 있다 |
| 재실행 | 탭을 다시 눌러 들어가니 정상 표시 |
| 확인 | 새로고침 **O** (탭 이동 불편 있음) |
| 판정 | **PASS** (기능 정상 · 사용성 흠) |

> 관리자가 목록을 확인하려고 누를 때마다 챗봇 탭으로 튕겨 나가면
> 여러 건을 확인할 때 번거롭다. 큰 문제는 아니지만 **고칠 것 목록에 적어 둔다.**

### C2. 동의어 5개 이상 등록 후 정상 작동

| 단계 | 내용 |
|---|---|
| 수행 | 표에 이미 8개가 등록돼 있어(개사·공개사·굴삭기·손평사·요보사·전기사·중장비면허·지게차면허) 새로 넣는 대신 **챗봇에서 실제로 작동하는지**를 확인했다 |
| 기대 | 5개 이상이 목록에 보이고 챗봇에서 작동 |
| 실제 | 목록 8개 확인. 챗봇에서 4개를 직접 시험해 모두 정상 작동<br>① `전기사 시험비` → 전기기능사 응시료 14,500원 / 106,000원 ✅<br>② `요보사 합격 기준` → 요양보호사 필기·실기 각 60% 이상 ✅<br>③ `손평사 접수 방법` → 손해평가사 큐넷 온라인 접수 ✅<br>④ `개사 시험 과목` → 공인중개사 1차 부동산학개론·민법, 2차 공인중개사법령 등 ✅ |
| 조치 | 문서가 예로 든 `위생 → 위생사` 는 **넣지 않았다.** "위생"은 두 글자짜리 짧은 말이라 `위생복`·`위생모` 같은 다른 낱말에도 걸려 엉뚱한 검색이 된다. 오늘 오후 실서비스 「말 바꾸기」 화면에 "두 글자보다 짧은 말은 넣지 마십시오" 검사를 넣은 것과 같은 이유다 |
| 재실행 | - |
| 확인 | **4/4 정상 작동** (등록은 8개) |
| 판정 | **PASS** |

> 동의어를 넣을 때는 **그 말이 다른 낱말 속에 들어 있지 않은지**를 봐야 한다.
> 짧을수록 위험하다. `개사` 도 `공인중개사` 안에 들어 있지만 확장 결과가 같은
> 자격증이라 문제가 되지 않는다. `위생` 은 `위생복`·`위생모` 처럼 **다른 뜻**의
> 낱말에 걸리므로 성격이 다르다.

---

## Won't - 오늘 테스트 안 함

- 4,705건 데이터 (도전3)
- TF-IDF 검색 (도전3)
- FAQ 추가 후 TF-IDF 인덱스 재구축 (도전4)

---

## 제출 요약

```
Must:      7/8 PASS   (M7 FAIL - 동의어 삭제)
Should:    4/4 PASS
Could:     2/2 PASS (선택)

테스트: 2026-08-20 (목) 13:46 ~ 14:18  (32분)
대상  : S3 https://32b927a8aeeef7c6fc.gradio.live
        (S3·S4 시험 중 재시작하여 https://37b714ebe56b6ab249.gradio.live 로 바뀜)
```

## 이번 테스트에서 나온 고칠 것

| # | 무엇 | 어디 | 심각도 |
|---|---|---|---|
| 1 | **동의어 삭제가 되지 않는데 "삭제 완료"라고 표시** | M7 | **높음** |
| 2 | 실습과 실서비스가 `synonyms` 표를 공유 — 실습에서 넣은 것이 배포 사이트에 반영됨 | M5 | **높음** |
| 3 | `전기사 → 전기기능사` 가 표에 남아 있음. 전기기사는 다른 자격증이라 실서비스에 부적합 | M5 | 중간 |
| 4 | 화면 설명이 "faq.json이 바뀌고"로 옛것 (실제 저장 위치는 Supabase 표) | M2 | 낮음 |
| 5 | 「목록 새로고침」을 누르면 챗봇 탭으로 화면이 넘어감 | C1 | 낮음 |
| 6 | 공유 URL 에 비밀번호가 없어 링크를 아는 사람은 누구나 FAQ 를 넣고 지울 수 있음 | M8 | 실습 범위에서는 허용 |

## 도전2에서 확인된 것

**관리 기능 한 바퀴가 돈다.** 웹 화면에서 넣은 FAQ 가 서버를 다시 켜지 않고 곧바로
검색에 잡히고(M2·M3), 삭제하면 사라진다(M4). 동의어도 넣으면 줄임말로 물어도
답한다(M5·M6). 공유 URL 로 바깥에서 접속한 상태에서도 모두 된다(M8).

**저장 위치를 표로 옮긴 것도 확인됐다.** `faq.json` 과 `synonyms.json` 을 폴더에서
아예 치운 채로 켰는데 FAQ 102건과 동의어 8개가 그대로 있었다(S3·S4).
파일이 없는데 데이터가 있으므로 표에서 왔다는 것 말고는 설명이 되지 않는다.

**그리고 "조용히 실패하는" 문제를 하나 더 만났다(M7).**
오후에 실서비스를 보호하려고 `synonyms` 표의 삭제 권한을 없앴는데,
그것이 실습 챗봇의 삭제를 막았다. Supabase 는 권한이 없을 때 에러가 아니라
"0건 처리"로 답하고, `store.py` 는 HTTP 200 만 보고 "삭제 완료"를 출력한다.
**한쪽을 안전하게 만든 조치가 다른 쪽을 조용히 망가뜨렸고, 화면은 성공이라고 말했다.**
QA 항목이 있었기 때문에 발견됐다.
