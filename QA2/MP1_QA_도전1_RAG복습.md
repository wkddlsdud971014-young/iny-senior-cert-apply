# 도전1 QA - RAG 복습 (S1+S2)

8/20(목) - 테스트 범위: 로컬 챗봇 실행 + share=True 공유 URL

> 작성자: 장인영 (iny)
> 테스트 시작: **13:28**   테스트 종료: **13:45**   (약 17분)
> 대상: S1 `http://localhost:7855` · S2 `https://e89c37ec2ddc7a0752.gradio.live`
> 결과: Must 6/6 · Should 3/3 · Could 보류1·부분1

---

## 테스트 작성법

한 항목당 아래 6칸을 채웁니다. "됐다/안 됐다"가 아니라 **무엇을 넣었고 무엇이 나왔는지** 씁니다.

```
수행: "한식조리기능사 시험비가 얼마예요?" 입력
기대: FAQ에서 찾은 내용을 Gemini가 답변
실제: [FOUND] 한식조리기능사 필기 14,500원, 실기 29,600원...
조치: -
재실행: -
판정: PASS
```

---

## Must - 반드시 통과

### M1. python app.py 실행 시 Gradio 화면이 뜬다

| 단계 | 내용 |
|---|---|
| 수행 | stage1_local_basic 폴더에서 `python app.py` 실행 |
| 기대 | 터미널에 `Running on local URL: http://127.0.0.1:7860` 표시, 브라우저에 챗봇 화면 |
| 실제 | `Running on local URL: http://127.0.0.1:7855` 표시. 브라우저에서 챗봇 화면이 떴다 |
| 조치 | 7860은 S2(공유형)가 쓰고 있어 `GRADIO_SERVER_PORT=7855`로 포트만 바꿔 띄웠다. 단계를 나란히 켜 두고 비교하기 위해서다 |
| 재실행 | - |
| 확인 | 화면이 뜨는가 **O** |
| 판정 | **PASS** |

### M2. FAQ에 있는 질문에 답변이 나온다

| 단계 | 내용 |
|---|---|
| 수행 | "한식조리기능사 시험비가 얼마예요?" 입력 |
| 기대 | [FOUND] 상태로 시험비 금액이 포함된 답변 |
| 실제 | `[ANSWERED] 한식조리기능사 필기시험 응시료는 14,500원이고 실기시험은 27,000원입니다. 큐넷에서 온라인으로 결제합니다.`<br>`출처: 한식조리기능사 - 한식조리기능사 응시료` |
| 조치 | - |
| 재실행 | - |
| 확인 | 답변 첫 줄 기록: **[ANSWERED] 한식조리기능사 필기시험 응시료는 14,500원이고 실기시험은 27,000원입니다** |
| 판정 | **PASS** |

> 상태 표시가 문서의 `[FOUND]`가 아니라 `[ANSWERED]`로 나온다. 배포본 `rag.py` 의 `answer_question()` 이 `status` 를 `ANSWERED`/`UNKNOWN` 으로 돌려주기 때문이다. 이름만 다르고 하는 일은 같다. 이 문서의 다른 항목도 같은 차이가 있다.

### M3. FAQ에 없는 질문에 "확인할 수 없음" 안내

| 단계 | 내용 |
|---|---|
| 수행 | "오늘 날씨 어때요?" 입력 |
| 기대 | [NOT_FOUND] 상태로 FAQ에서 확인할 수 없다는 안내 |
| 실제 | `[UNKNOWN] 제공된 FAQ에서 확인할 수 없는 내용입니다.`<br>`출처: 없음` |
| 조치 | - |
| 재실행 | - |
| 확인 | 상태 표시: **[UNKNOWN]** (문서의 NOT_FOUND에 해당) |
| 판정 | **PASS** |

> Gemini 는 날씨를 그럴듯하게 지어낼 수 있는 모델이다. 그런데도 답하지 않았다.
> `rag.py` 의 `retrieve()` 가 점수 2점 미만이면 근거를 하나도 넘기지 않고,
> `build_prompt()` 가 "근거로 답할 수 없으면 UNKNOWN 이라고만 답하라"고 못 박기 때문이다.
> 출처가 "없음"으로 나오는 것이 근거를 못 찾았다는 표시다.

### M4. share=True로 공유 URL이 생성된다

| 단계 | 내용 |
|---|---|
| 수행 | stage2_share_basic 폴더에서 `python app.py` 실행 |
| 기대 | 터미널에 `Running on public URL: https://xxxxx.gradio.live` 표시 |
| 실제 | `Running on local URL: http://127.0.0.1:7860`<br>`Running on public URL: https://e89c37ec2ddc7a0752.gradio.live` |
| 조치 | 처음에는 공개 URL이 화면에 안 찍혔다. `frpc` 프로세스는 떠 있어 터널은 연결된 상태였고, 파이썬이 출력을 모아 두는(버퍼링) 탓에 주소만 안 보인 것이었다. `python3 -u app.py`(출력 즉시 내보내기)로 다시 실행해 주소를 확인했다 |
| 재실행 | `-u` 옵션으로 재실행 → 공개 URL 정상 표시 |
| 확인 | 공유 URL 기록: `https://e89c37ec2ddc7a0752.gradio.live` |
| 판정 | **PASS** |

> "안 되는 것"과 "안 보이는 것"은 다르다. `frpc` 프로세스가 떠 있는 것을 확인해
> 연결은 됐고 출력만 문제라고 좁힐 수 있었다.

### M5. 공유 URL을 다른 기기에서 열면 챗봇이 뜬다

| 단계 | 내용 |
|---|---|
| 수행 | M4의 URL을 **폰**에서 연다 (와이파이가 아니라 **5G 이동통신망**) |
| 기대 | 챗봇 화면이 뜨고 질문을 입력할 수 있다 |
| 실제 | 폰 브라우저에서 챗봇 화면이 떴고, "한식조리기능사 시험비가 얼마예요?"에 `[ANSWERED] 한식조리기능사 필기시험 응시료는 14,500원이고 실기시험은 27,000원입니다` 로 답했다 |
| 조치 | - |
| 재실행 | - |
| 확인 | 다른 기기에서 정상 **O** |
| 판정 | **PASS** |

> 와이파이가 아니라 5G 로 접속해 확인했다. 같은 공유기를 쓰는 것이 아니므로
> 바깥 인터넷에서 들어온 것이 확실하다.
> S2 는 S1 과 `rag.py`·`gemini.py`·`faq.json` 이 완전히 같고 마지막 줄의
> `share=True` 하나만 다르다. 프로그램을 새로 만든 것이 아니라 이미 돌고 있는
> 프로그램에 바깥에서 들어올 문을 하나 낸 것이다.

### M6. 다른 자격증으로도 질문한다

| 단계 | 내용 |
|---|---|
| 수행 | "지게차 접수는 어디서 해요?" 입력 (폰 · 5G) |
| 기대 | 지게차운전기능사 관련 답변 |
| 실제 | `[ANSWERED] 지게차운전기능사는 큐넷(Q-Net) 홈페이지에서 온라인으로 접수합니다.`<br>`출처: 지게차운전기능사 - 지게차운전기능사 접수방법` |
| 조치 | - |
| 재실행 | - |
| 확인 | 답변에 "지게차" 또는 "큐넷" 포함 **O** (둘 다 포함) |
| 판정 | **PASS** |

> 질문은 "지게차"라고만 했는데 답은 정식 명칭인 "지게차운전기능사"로 나왔다.
> FAQ 문서에 적힌 말로 바꿔 찾은 결과다.

---

## Should - 되어야 하지만 FAIL이면 내일 수정

### S1. 예시 질문 8개 중 6개 이상 정상 답변

| 단계 | 내용 |
|---|---|
| 수행 | 화면 아래 Examples 버튼 8개를 하나씩 눌렀다 |
| 기대 | 6개 이상 [FOUND]로 관련 답변 |
| 실제 | **ANSWERED 7 / UNKNOWN 1**<br>① 한식 시험비 → 14,500원·27,000원 ✅<br>② 지게차 접수처 → 큐넷 온라인 ✅<br>③ 굴착기 시험과목 → 굴착기 점검·시운전·주행·작업방법·안전관리 ✅<br>④ 요양보호사 합격기준 → 필기/실기 각 60% 이상 ✅<br>⑤ 공인중개사 환불 → 기간 내 100%, 5일 전 50%, 4일 전 불가 ✅<br>⑥ 위생사 응시자격 → 전문대 이상 관련학과 졸업 ✅<br>⑦ **손해평가사 1차 2차 차이 → `[UNKNOWN]` 출처: 없음** ❌<br>⑧ 전기기능사 계산기 → 공학용 가능, 프로그래밍 기능 금지 ✅ |
| 조치 | ⑦의 원인을 확인했다. `faq.json` 40건 안에 손해평가사는 5건(응시료·접수방법·시험과목·합격기준·환불)뿐이고 **"1차 2차 차이" 항목 자체가 없다.** 근거가 없는데도 답했다면 그것이 결함이다. 지어내지 않고 UNKNOWN 을 낸 것이 올바른 동작이므로 코드는 고치지 않았다 |
| 재실행 | - |
| 확인 | FOUND 수: **7/8** (기준 6개 이상) |
| 판정 | **PASS** |

> 예시 질문 목록(app.py 의 EXAMPLES)과 실제 데이터(faq.json 40건)가 어긋나 있다.
> 데이터에 없는 것을 예시로 걸어 둔 것이므로, 고칠 곳은 챗봇이 아니라 예시 목록이거나
> 데이터 쪽이다. 참고로 100건짜리 데이터(S3)에는 "손해평가사 1차 2차" 항목이 있다.
> 데이터가 늘면 답할 수 있게 된다는 것은 도전3에서 다시 확인한다.

### S2. API 키 없이 실행 시 에러 메시지

| 단계 | 내용 |
|---|---|
| 수행 | `.env` 를 `.env.잠깐치움` 으로 바꿔 키를 없앤 뒤, 포트 7899로 실행하고 "한식조리기능사 시험비가 얼마예요?" 입력 |
| 기대 | [ERROR]로 시작하는 에러 메시지 (화면이 깨지지 않음) |
| 실제 | `[ERROR] RuntimeError: GOOGLE_API_KEY가 없습니다. .env 파일에 키를 넣어주세요.`<br>화면은 그대로 살아 있고 입력칸·예시 버튼도 정상. 터미널도 죽지 않았다 |
| 조치 | 시험이 끝나고 `.env.잠깐치움` 을 `.env` 로 되돌렸다. 키가 그대로 들어 있는 것까지 확인했다 |
| 재실행 | 되돌린 뒤 정상 답변이 나오는 것을 확인 |
| 확인 | 에러 메시지 기록: **[ERROR] RuntimeError: GOOGLE_API_KEY가 없습니다. .env 파일에 키를 넣어주세요.** |
| 판정 | **PASS** |

> 보는 것은 "키가 없을 때 프로그램이 죽는가"이다. 실제 서비스에서 키가 만료되거나
> 잘못 들어가는 일은 흔한데, 그때 화면이 하얗게 죽으면 사용자는 원인을 알 수 없다.
> 여기서는 `gemini.py` 가 키가 없을 때 `RuntimeError` 를 던지고,
> `app.py` 의 `chat()` 이 그것을 `try/except` 로 받아 `[ERROR] ...` 문자열로 돌려준다.
> 그래서 화면은 살아 있고 무엇이 문제인지도 읽을 수 있다.

### S3. 서버를 끄고 다시 켜도 같은 결과

| 단계 | 내용 |
|---|---|
| 수행 | Ctrl+C로 서버 중지 후 다시 실행하고 M2와 같은 질문 입력 |
| 기대 | 같은 질문에 같은 답변 |
| 실제 | `[ANSWERED] 한식조리기능사 필기시험 응시료는 14,500원이고 실기시험은 27,000원입니다. 결제는 큐넷에서 온라인으로 합니다.`<br>`출처: 한식조리기능사 - 한식조리기능사 응시료`<br>→ M2 와 **금액·출처·문장까지 완전히 동일** |
| 조치 | 처음 재실행했을 때 터미널에 `http://127.0.0.1:7856` 이 떴다. 7855를 지정했는데도 다른 번호로 뜬 것은 이전 서버가 그 포트를 아직 잡고 있었기 때문이다. Gradio 는 지정한 포트가 쓰이고 있으면 **에러 없이 조용히 옆 번호로 옮겨간다.** 그대로 뒀다면 "껐다 켠 것"이 아니라 서버가 하나 더 뜬 상태에서 시험한 셈이 된다. 이전 프로세스를 끝내고 7855가 비었는지 확인한 뒤 다시 실행했다 |
| 재실행 | 포트 정리 후 재실행 → `http://127.0.0.1:7855` 로 정상 기동, 답변 일치 확인 |
| 확인 | 답변 일치 **O** |
| 판정 | **PASS** |

> 재시작해도 금액이 그대로라는 것은 답이 **데이터에서 나온다**는 뜻이다.
> 모델이 지어내고 있다면 재시작할 때마다 숫자가 흔들린다.
>
> 포트가 조용히 바뀌는 동작은 오늘 두 번 걸렸다. 오전에도 S3 를 껐다고 생각했으나
> 예전 프로세스가 7865를 잡고 있어 Supabase 전환 전 버전이 계속 돌고 있었다.
> **터미널에 찍힌 포트 번호를 확인하는 것이 필요하다.**

---

## Could - 있으면 좋지만 필수 아님

### C1. 공유 URL이 72시간 유지된다

| 단계 | 내용 |
|---|---|
| 수행 | M4에서 받은 URL을 내일 다시 열어본다 |
| 기대 | 서버가 켜져 있으면 72시간 동안 접속 가능 |
| 실제 | **오늘은 확인 불가.** 8/20 현재 생성 후 정상 작동 중. 8/21 에 다시 열어 확인 예정 |
| 조치 | - |
| 재실행 | - |
| 확인 | 24시간 후 접속: **내일(8/21) 확인 예정** |
| 판정 | **보류 (내일 확인)** |

> 이 주소는 72시간이 보장된 것이 아니라, **내 노트북에서 프로그램이 계속 돌고 있어야**
> 살아 있다. 노트북을 덮거나 터미널을 닫으면 그 즉시 끊긴다. 내일 안 열린다면
> 그것은 고장이 아니라 서버가 꺼진 것이다. 계속 살아 있어야 하는 주소가 필요하면
> Vercel 에 올린 접수 사이트 쪽을 쓴다.

### C2. 폰에서 챗봇 화면이 읽힌다

| 단계 | 내용 |
|---|---|
| 수행 | 공유 URL을 폰 브라우저에서 연다 (5G) |
| 기대 | 가로 스크롤 없이 화면이 보이고 질문 입력 가능 |
| 실제 | 질문 입력과 답변은 잘 된다. 다만 **가로로 밀어야 보이는 부분이 있고**, 화면이 흔들리며, **채팅 창이 너무 작아** 답변이 중간에 잘린다(`출처: 한식조리기능사 …` 가 끊겨 보임). 예시 버튼도 한 줄에 하나씩만 보여 스크롤이 길다 |
| 조치 | 고치지 않았다. 이 화면은 Gradio 가 자동으로 만든 것이라 우리 CSS 로 손댈 수 없다. 어제 QA1 의 "휴대폰 글자 밀림"(C1)은 우리가 만든 사이트라 고칠 수 있었지만, 이것은 성격이 다르다 |
| 재실행 | - |
| 확인 | 모바일 정상 **△** (쓸 수는 있으나 읽기 불편) |
| 판정 | **부분 통과** — 기능은 되고 화면만 불편 |

> 실습용 도구와 납품용 화면의 차이가 드러나는 지점이다.
> Gradio 는 화면을 자동으로 만들어 주는 대신 그 모양을 마음대로 바꿀 수 없다.
> 시니어가 실제로 쓸 화면이라면 이 상태로는 부족하다.
> 그래서 시니어용 챗봇은 Gradio 를 그대로 쓰지 않고, 직접 만든 사이트
> (`/faq.html`)에 넣어 글자 크기와 여백을 맞춘 것이다.

---

## Won't - 오늘 테스트 안 함 (다른 도전 범위)

- FAQ 추가/삭제 (도전2)
- 동의어 사전 (도전2)
- 4,705건 데이터 (도전3)
- TF-IDF 검색 (도전3)

---

## 제출 요약

```
Must:      6/6 PASS
Should:    3/3 PASS
Could:     0/2 PASS (선택) - C1 내일 확인 예정, C2 부분 통과

테스트: 2026-08-20 (목) 오후
대상  : S1 http://localhost:7855
        S2 https://e89c37ec2ddc7a0752.gradio.live
```

## 이번 테스트에서 나온 것

| # | 무엇 | 어디 | 고칠 곳 |
|---|---|---|---|
| 1 | 상태 표시가 문서와 다름 (`FOUND`/`NOT_FOUND` → `ANSWERED`/`UNKNOWN`) | 전 항목 | 문서 표현 문제. 코드는 그대로 둠 |
| 2 | 예시 질문에 데이터가 없는 항목이 섞여 있음 (손해평가사 1차 2차) | S1 | `app.py` 의 EXAMPLES 또는 `faq.json` |
| 3 | 폰에서 채팅 창이 작아 답변이 잘림 · 가로 스크롤 발생 | C2 | Gradio 자동 화면이라 수정 불가. 납품용은 `/faq.html` 을 씀 |
| 4 | 지정한 포트가 쓰이고 있으면 Gradio 가 조용히 다른 포트로 옮겨감 | S3 | 코드 문제 아님. 재시작할 때 터미널의 포트 번호를 확인할 것 |
| 5 | 공유 URL 은 노트북이 켜져 있어야만 살아 있음 | C1 | 설계상 한계. 계속 살아 있어야 하면 Vercel 배포본을 씀 |

## 도전1에서 확인된 것

`share=True` 한 줄로 바깥에서 들어올 주소가 생긴다. S1 과 S2 는
`rag.py`·`gemini.py`·`faq.json` 이 완전히 같고 마지막 줄만 다르다.
**공개는 프로그램을 새로 만드는 일이 아니라 이미 돌고 있는 프로그램에 문을 내는 일이다.**

그리고 근거를 못 찾으면 답하지 않는다는 원칙이 세 번 확인됐다.
날씨 질문(M3), 데이터에 없는 손해평가사 1차 2차(S1), 키가 없을 때(S2) 모두
지어내지 않고 상태를 밝히며 멈췄다.
